# AI 生成器平台 — AI 核心端细节（AI Service）

> 本文档定义 AI 服务（FastAPI / Python，仅内网、持模型 Key）的实现细节：**模型抽象层、Router+Skill 编排、generate_site 内层多 agent、思考链路事件、Skill/Tool 注册表与本地落地、RAG、记忆闭环、COS 预览投递、优化方案（1-C~5-C）、资源层连接池**。
> 跨服务架构见《项目总览》；前端契约见《前端细节》；业务接口见《业务端细节》。
> 全文以「当前方案」表述，不含历史修订记录。代码已落地于 `backend/ai_service/app/`（见 §8）。

---

## 1. 技术栈与职责

- **框架**：FastAPI（Python）+ LangGraph + SQLAlchemy(async，若需本地表）。
- **职责**：模型抽象层（Provider 注册表）、Router+Skill 路由、generate_site 内层 LangGraph 轻量多 Agent、RAG（Chroma）、流式生成。**独享模型 API Key**。
- **数据归属**：Chroma（向量）+ Redis（队列 / 缓存）。MySQL 归业务服务（Trace/Feedback 由业务落库；本服务仅产出事件经 SSE 透传）。
- **对外**：仅对业务服务暴露。端点 `/generate`（SSE）、`/models`、`/skills`、`/tools`、`/registry`（调试）。

---

## 2. 模型抽象层（Provider / FallbackRouter）

目标：新增一个模型 = 写一个适配器 + 注册，前端与业务服务无感。

```python
from abc import ABC, abstractmethod
from typing import AsyncGenerator

class BaseLLMProvider(ABC):
    id: str
    label: str
    @abstractmethod
    def chat_stream(self, messages: list[dict], **kwargs) -> AsyncGenerator[str, None]: ...

PROVIDERS: dict[str, BaseLLMProvider] = {
    "deepseek": DeepSeekProvider(api_key=settings.DEEPSEEK_KEY),
    "qwen":     QwenProvider(api_key=settings.QWEN_KEY),
    "hy3":      HY3Provider(api_key=settings.HY3_KEY),
}
```

- **HY3**：OpenAI 兼容，`base_url=https://tokenhub.tencentmaas.com/v1`，model=`hy3`。
  - **关键坑（2026-07-18 修复）**：`HY3_API_KEY` 必须是腾讯 TokenHub 控制台（https://console.cloud.tencent.com/tokenhub/apikey）创建的**真正 API Key（形如 `sk-...` 或令牌串）**；文档早前记的 `TokenHub_QCSLinkedRoleInInitialization` 只是「关联角色名」，不是可用 Key，直连会被 `401002 API Key 不存在/签名校验失败` 拒绝。Demo 测试可用 `HY3_API_KEY_DEMO`（示例临时 key）。
- **扩展**：新增模型只需实现 `BaseLLMProvider` 并注册一行。
- **配置加载（2026-07-18 修正）**：`config.py` 用 `Path(__file__).resolve().parents[3] / ".env"` **绝对路径**加载项目根 `.env`，不受服务启动目录影响。此前用相对 `env_file=".env"`，本地从 `backend/ai_service/app` 启动时会找不到根 `.env` → 三个模型 Key 全空 → hy3 调 TokenHub 带空 Key 触发 401 鉴权失败。业务端 `config.py` 同样改为根绝对路径（避免回落 SQLite / 默认值）。
- **FallbackRouter（2-C）**：在 `PROVIDERS` 之上加降级路由。
  - 主模型由用户 **套餐/层级(plan/tier)** 决定（免费用便宜模型如千问，付费可用贵的如 DeepSeek/HY3）。
  - 用户前端可**手动勾选模型优先级（主 + 备）**，后端按指定顺序回退。
  - 主模型 429/超时/5xx 时按序回退；发生回退时思考事件带 `degraded: true`，前端显示「已降级」。
  - **默认降级序：HY3 → Qwen → DeepSeek**（用户指定可覆盖）。
- **端点**：
  - `POST /generate`（SSE）：内部经 Redis `queue:generate` + Worker 产出；思考事件同 §5。
  - `GET /models`：返回 `[{id, label}]` 供前端选择器。

---

## 3. 编排架构（Router + Skill + Hybrid）

- **外层 Router（两级意图分类 + Skill 分发）**：Router 先调 `intent_classifier.classify()` 做一次轻量 LLM 调用，同时输出 `{level1, level2, confidence, industry}` 四元组：
  - **level1**（6 类）：`learn`（学习理解）| `code`（编码实战）| `build`（建站生成）| `doc`（文档方案）| `translate`（翻译转换）| `unsupported`
  - **level2**（17 子类）：在 level1 下细分，如 `learn/explain`（概念解释）、`code/fix`（修复 Bug）、`build/game`（互动游戏）等，完整矩阵见 docs/06
  - **industry**（12 行业）：`restaurant`/`ecommerce`/`gov`/`edu`/`health`/`finance`/`game`/`personal`/`corp`/`tech`/`media`/`other`，仅 build/doc 时启用，注入 Planner 设计约束
  - 分类失败时走关键词兜底（含 level2 细判 + industry 探测）
  - Router 按二维映射 `(level1, level2) → skill_name` 分发到对应 Skill handler；handler 内部根据 `level2` 切换 System Prompt 模式（如 debug/compare/fix/refactor/game 各有专属 prompt）
- **内层仅对 `generate_site` 保留轻量多 Agent**：`Planner → Coder → Reviewer`，Planner 接收 industry 设计约束注入，Coder 根据 `level2=game` 切换 Three.js 游戏模式。
- **其余 Skill 单次 LLM 直出**：`write_code` / `generate_doc` / `explain` / `rag_retrieve` 无需多 agent 协作。
- **🆕 多意图拆分（v0.8.x）**：用户单条提问若含多个意图，Router 之后由 `intent/splitter.py` 拆为多个 `SubTask`，经 **DAG 编排器**（`core/orchestrator.py`）按依赖关系分层——同层 `asyncio.gather` 并行、跨层串行；各子任务独立 `run_skill` 产出，最终由**合并器**汇成一条连贯回复（`merge` 事件 + `__merge__` token）；**风险分级门控**：HIGH=拒绝、MEDIUM=确认后执行、LOW=自动执行（见 `run_safety`）。前端渲染见《前端细节》§14。

**服务间调用关系**

```
业务服务 /api/chat (SSE) ─┬─ [WebLLM 旁路] 前端本地首过(同构事件,失败回退)
                          └─ 内网 → AI 服务 /generate (SSE, 经 Redis 队列+Worker)
                                └─ Router(意图/鉴权/配额) → 分发 Skill
                                      ├─ generate_site → {Planner→Coder→Reviewer}(LangGraph)
                                      ├─ write_code / generate_doc / explain → 单次 LLM
                                      ├─ rag_retrieve → Chroma(语义缓存)
                                      └─ 模型抽象层 + FallbackRouter → DeepSeek/Qwen/HY3
                                      └─ Redis(queue:generate / 进度 / 缓存)
```

---

## 4. generate_site 内层（LangGraph + Plan-and-Execute + Reflexion）

内层状态图（LangGraph）：

```python
from langgraph.graph import StateGraph, END

class SiteState(TypedDict):
    spec: str          # Planner 产出(与 Coder 共享,不重复喂原始对话)
    html: str
    reviews: int
    passed: bool

def planner(state): ...      # 需求拆解 → spec
def coder(state): ...        # spec + style 约束 → html
def reviewer(state): ...     # 静态分析 + LLM 自审 → passed / 修改建议

graph = StateGraph(SiteState)
graph.add_node("planner", planner)
graph.add_node("coder", coder)
graph.add_node("reviewer", reviewer)
graph.add_edge("planner", "coder")
graph.add_edge("coder", "reviewer")
graph.add_conditional_edges("reviewer", route_after_review,
    {"pass": END, "retry": "coder"})   # ≤3 轮,避免死循环
app = graph.compile()
```

- **演进姿态（Plan-and-Execute + Reflexion）**：
  1. Planner 产出结构化页面规格（板块 / 布局 / 风格 / 技术选型）→ 作为 `plan` 事件推前端思考面板；
  2. Coder 严格按 `plan` 生成单文件 HTML（共享 plan 上下文）；
  3. Reviewer 按计划逐条核对 + 静态分析（§12 的 3-C）；不达标进入 **Reflexion 循环**：失败原因写入短期记忆 → 反思 → 重写（≤3 次）。
- 其余 Skill 是普通函数，单次 `PROVIDERS[model_id]` 调用即返回（无状态图）。
- RAG（§9）在 `generate_site` / `generate_doc` 的 Planner/Coder 前注入组件与记忆上下文（Chroma）。
- 各调用统一走 §2 模型抽象层 + FallbackRouter。

---

## 5. 思考链路事件（Thinking Trace）

Router 与 generate_site 内层在协作中产出**结构化思考事件**，经业务服务转发为 SSE 帧（《前端细节》§3）：

| 事件 | 触发点 | 负载 |
|------|--------|------|
| intent | 分类完成 | `{level1, level2, label, level1_label, level2_label, confidence, industry}` |
| node:enter_router | 收到需求 | `{stage: "enter_router"}` |
| node:dispatch | Router 分发 | `{stage: "dispatch", skill: "generate_site"}` |
| node:enter_planner | 进入规划 | `{stage: "enter_planner"}` |
| think:planner | Planner 产出 | `{stage: "planner", content: "..."}` |
| plan | 计划卡片 | `{title, goal, steps}` |
| node:enter_coder | 进入编码 | `{stage: "enter_coder"}` |
| token | 产物流 | 逐段 HTML/Markdown 文本 |
| node:enter_reviewer | 进入评审 | `{stage: "enter_reviewer", attempt: N}` |
| think:reviewer | Reviewer 结论 | `{stage: "reviewer", passed: bool, comment: "..."}` |
| node:previewing | 投递预览 | `{stage: "previewing"}` |
| node:preview | 预览就绪 | `{url: "COS直链", fallback: "srcdoc"}` |
| retry | 模型不可用 | `{failed, suggested, message}` |
| unsupported | 不支持意图 | `{message: "..."}` |
| node:done | 完成 | — |

> 意图事件 `intent` 带两级标签，前端 ThoughtTrail 显示为 "🧠 已识别: 学习理解 → 排查报错"。

---

## 6. SkillRegistry（技能注册表）

核心是一个注册表 `SkillRegistry: dict[name → SkillEntry]`：

- `name`：技能名（如 `generate_site`）
- `intent_tags`：意图标签（辅助 Router 匹配，如 `["site","网页","页面"]`）
- `handler`：处理函数，或已编译的 LangGraph app
- `is_graph`：是否多 agent 状态图（`True` 时 `handler` 为 `graph.compile()`）
- `description`：给 Router / 前端展示的说明

**Router 分发流程**：
1. `intent_classifier.classify()` 调一次 LLM 出 `{level1, level2, confidence, industry}`，失败走关键词兜底；
2. `router.skill_for(level1, level2)` 按二维映射查 `INTENT_SKILL_MAP` 得 `skill_name`；
3. `runner.run_skill(skill_name, ..., intent_info={level1, level2, ...})` 调 handler，handler 内按 `level2` 切 System Prompt 模式，`industry` 注入 Planner。

**引入新意图子类**：在 `INTENT_SKILL_MAP` 加 `(level1, level2) → skill` 映射 → Router 自动识别，**无需改 handler**（handler 收不认识的 `level2` 时用默认 prompt）。

**动态发现（M2+）**：扫描 `ai_service/skills/` 目录自动注册，支持运营 / 用户上传自定义 Skill 热插拔。

> **变更补充（2026-07-22）：决策自治 + 选择解析**
> - 意图管道已迁移到 `intent/pipeline.py` 的 `classify_v2()` + `_aggregate()`（非旧 `router.skill_for`）：汇总器输出 `decision ∈ {route, block, confirm, split}`（**已移除 `options` 阻塞决策**）。
> - **工具路由不再阻塞用户**：低置信时 `_aggregate` 直接 `decision="route"` 选 `tools.skills[0]`（top1），其余候选通过 `plan` 里的 `alternatives` 动作以 **非阻塞 `alternatives` SSE 事件** 提示前端「已选 X，可切换 Y」，系统照常执行 top1。
> - **选择解析（修 "B" 未被识别）**：新增 `intent/selection.py` —— 用户回 `"B"`/`"2"`/`"选B"`/`"第二个"`/`"用 explain"` 时，若 `pending_options`（按 `conversation_id`，内存 dict 兜底，TTL 1800s）命中，则 `classify_v2` **最前短路**直接路由到对应 skill，不再当新 query 重分类。`pending_options` 存**完整有序候选 `[top1, *alts]`**，使 "A"=当前选择(无操作)、"B"=alts[0]。
> - **语义澄清**：`intent/semantic.py` 的 LLM 分类 prompt 增加裁决规则——意图模糊时自行选最具体/上游 skill（build 默认 site、learn 默认 explain），confidence 给 ≥0.6，不把选择推给用户。
> - **保留**：`requirement_agent` 的「方案多选」属业务级决策，仍走 `options` 事件 + 前端单选弹窗（不在本次管道级修复范围）。
> - **日志**：`logging_config.TraceIdFilter` 用 `contextvars` 给每条日志注入 `[trace=..]`，贯穿 `/generate → Worker[1/6~6/6] → 技能 → 完成`，grep 单一 trace 可见全链路。

---

## 7. ToolRegistry（工具注册表）

**Skill vs Tool 区别**：
- **Skill**：对外暴露的「一个完整任务能力」，由 Router 按意图分发（如 `generate_site` / `explain`）；一次调用产出完整结果。
- **Tool**：Skill 内部 agent 可调用的「原子操作」，经 LLM **function calling** 暴露给模型，模型自主决定何时调用（如向量检索、COS 上传、代码执行、HTTP 请求）。

核心是一个注册表 `ToolRegistry: dict[name → ToolEntry]`：

- `name`：工具名（如 `rag_retrieve`）
- `schema`：JSON Schema（参数名 / 类型 / 描述），用于 function calling 暴露给模型
- `func`：实际执行函数
- `scope`：`internal`（仅内部 agent 调用）/ `user_exposed`（用户可见可触发，如 UI 按钮 / 显式指令）
- `risk`：`safe` / `dangerous`（危险工具需权限校验 / 沙箱隔离）

**Tool 来源分类**：
- **A. 内置工具**：代码内用 `@tool` 装饰器实现并注册（如 `rag_retrieve` / `cos_upload` / `web_search` / `code_run`）。**MVP 全部走这层**。
- **B. 运营 / 用户贡献工具（M2+）**：一个 Python 文件 + schema 声明，放入 `ai_service/tools/` 目录；启动扫描自动注册。
- **C. 第三方 MCP 工具（M3+）**：接外部 MCP server，协议自动将 MCP 能力转为 tool schema（如高德地图、GitHub）；MVP 不接。

**`rag_retrieve` 双入口**：对外是 Skill（用户说「检索我的组件库」→ Router 分发返回上下文）；对内是 Tool（`generate_site` 内部 Planner 检索上下文时调用增强质量）。一个实现、`scope=internal` 供内部、`scope=user_exposed` 供 UI / 显式指令触发。

**引入新内置 Tool**：写函数 + `@tool(name=, schema=, scope=, risk=)` → `register_tool(...)` → Skill 内部 agent 经 function calling 即可见，Router / 核心无需改动。

---

## 8. 本地 Skills / Tools 落地清单（已落地代码）

§6 / §7 的注册表契约已落到 `backend/ai_service/app/`，严格对齐，引入成熟开源库 + 成熟 agent 模式，使注册表从「空」变为「可用、可扩展」。

**目录结构**（均位于 `backend/ai_service/app/`）：

```
registry/                 # 注册表核心
  skill_registry.py       # SkillRegistry + register_skill + match(意图子串匹配)
  tool_registry.py        # ToolRegistry + register_tool + @tool 装饰器(含 schema 推断)
  __init__.py
tools/                    # 来源 A 内置工具(@tool 注册,导入即注册)
  rag_retrieve.py  web_search.py  fetch_url.py  cos_upload.py
  browser_screenshot.py  image_generate.py  html_validate.py  file_io.py
  __init__.py
skills/                   # Skill(register_skill 注册)
  generate_site.py        # 核心建站(Planner→Coder→Reviewer, LangGraph, is_graph=True)
  write_code.py  generate_doc.py  explain.py
  rag_retrieve_skill.py   # 与 tools.rag_retrieve 双入口
  __init__.py
registries.py             # bootstrap():导入 skills/tools 包完成注册 + 目录扫描热插拔
router.py                 # detect_intent(规则匹配) + dispatch(分发到 Skill,流式透传)
```

**已落地 Skill 清单**：

| Skill | intent_tags（节选） | 类型 | 成熟来源 / 模式 | 文件 |
|-------|------------------|------|----------------|------|
| `generate_site` | site/网页/页面/落地页/官网/博客 | 图(is_graph) | LangGraph + Plan-and-Execute + Reflexion | `skills/generate_site.py` |
| `write_code` | 代码/code/脚本/函数 | 单次 LLM | ReAct 简化形态 | `skills/write_code.py` |
| `generate_doc` | 文档/doc/readme/教程 | 单次 LLM | 单次 LLM 直出 | `skills/generate_doc.py` |
| `explain` | 解释/问答/为什么 | 单次 LLM | 单次 LLM 直出 | `skills/explain.py` |
| `rag_retrieve` | 检索/组件库/记忆/搜一下 | 单次(双入口) | 复用 `tools.rag_retrieve` | `skills/rag_retrieve_skill.py` |

**已落地 Tool 清单（来源 A 内置）**：

| Tool | scope | risk | 成熟来源 | 文件 |
|------|-------|------|----------|------|
| `rag_retrieve` | user_exposed | safe | Chroma + Qwen text-embedding（§9） | `tools/rag_retrieve.py` |
| `web_search` | internal | safe | Tavily / Serper / DuckDuckGo lite（三级回退） | `tools/web_search.py` |
| `fetch_url` | internal | safe | httpx + BeautifulSoup4 | `tools/fetch_url.py` |
| `cos_upload` | internal | safe | 腾讯云 cos-python-sdk-v5（§11） | `tools/cos_upload.py` |
| `browser_screenshot` | internal | dangerous | Playwright（无头 Chromium） | `tools/browser_screenshot.py` |
| `image_generate` | user_exposed | safe | OpenAI 兼容 images API | `tools/image_generate.py` |
| `html_validate` | internal | safe | 标准库 html.parser | `tools/html_validate.py` |
| `file_write` / `file_read` | internal | safe | 标准库 pathlib | `tools/file_io.py` |

**关键工程约定**：
1. **重依赖懒加载**：`chromadb` / `cos-python-sdk-v5` / `playwright` 均在**函数体内** `import`；缺包时该工具返回清晰错误（`{"ok":False,"error":"..."}`），**不阻断包导入与整体注册**。
2. **导入即注册**：`tools/__init__.py` / `skills/__init__.py` 导入各模块时执行 `@tool` / `register_skill`；`registries.bootstrap()` 导入两包即完成全量注册。
3. **目录扫描热插拔（M2+）**：`bootstrap()` 额外扫描 `tools/` / `skills/` 目录，直接丢 `.py` 文件即生效；单文件异常被捕获，不阻断整体。
4. **Router 默认兜底**：`detect_intent` 无命中时兜底到 `generate_site`，保证任何输入都有响应。
5. **调试端点**：AI 服务 `GET /skills`、`GET /tools`、`GET /registry`，查看已注册能力（admin 看板可对接）。

**如何新增（开闭原则）**：
- 新内置 Tool：`tools/` 下新建模块，`@tool(name=, schema=, scope=, risk=)` 装饰函数，在 `tools/__init__.py` 加一行 import；模型经 function calling 即可见。
- 新 Skill：`skills/` 下新建模块，调 `register_skill(name=, intent_tags=, handler=, is_graph=)`；Router 自动识别分发。
- 新多 Agent：用 LangGraph 定义图，`register_skill(..., is_graph=True, handler=graph.compile())`，对外仍是一个 Skill。
- 运营/用户贡献（M2+）：直接把 `.py` 丢进 `tools/` 或 `skills/`，重启即被 `bootstrap()` 扫描注册（`risk=dangerous` 工具须走沙箱 + 权限白名单）。

**验证（本地实测）**：`bootstrap()` 实测注册 **5 Skill + 9 Tool**；`/generate` 经 Router 分发；`web_search` 无 key 走 DuckDuckGo 实返回 5 条；`rag_retrieve` / `cos_upload` 缺依赖时优雅返回错误而非崩溃。契约完全对齐 §6 / §7。

---

## 9. RAG 设计（Chroma，归 AI 服务）

两个用途，两个独立 Collection：

| Collection | 内容 | 何时写入 | 何时检索 |
|-----------|------|---------|---------|
| `components` | 优质组件 / 页面模板片段 + 描述 | 运营预置 / 沉淀 | 生成前：检索 Top-K 拼进 Prompt，提升质量 |
| `memory` | 用户历史项目摘要 / 对话要点 | 每次生成后异步写入 | 生成前：检索该用户相关历史，注入上下文 |

**流程**：用户需求 → Embedding → Chroma 检索（components + memory）→ 拼装增强 Prompt → 调模型 → 生成 → 异步回写 memory。

- Embedding 模型：**Qwen text-embedding**（复用 DashScope，与千问同生态）。M0 不接向量检索，M1 做 RAG/components 检索时落地。
- Chroma 初始化：`client.get_or_create_collection(name=...)`（§13）。

> **⚠ 未实装（M1 未落地）**：`generate_site` 当前**未调用** `rag_retrieve` / 不注入 components/memory 上下文——`generate_site.py` 内无 RAG 检索代码。仅 `rag_retrieve` 作为独立 Skill/Tool 存在（用户显式「检索组件库」时可触发），但未自动增强生成。Chroma 集合尚未预置数据。完整 RAG 增强待接入。

---

## 10. 记忆系统闭环

本系统的「记忆」不只有 Chroma，而是一个**多层读写闭环**。

**记忆分层**：

| 层 | 载体 | 内容 | 生命周期 | 读写时机 |
|----|------|------|---------|---------|
| 短期记忆 | MySQL `Trace` / `TraceEvent`（业务服务） | 本次会话完整事件流 | 一次提问；结束落库可回放 | 实时写；不进向量库 |
| 长期记忆（用户） | Chroma `memory` | 用户历史项目摘要 / 对话要点 | 跨会话、跨项目 | 生成后异步回写；生成前检索 |
| 组件记忆 | Chroma `components` | 优质组件 / 模板库 | 长期（运营沉淀） | 运营预置；生成前检索注入 |
| 语义缓存 | Chroma `cache:gen` | 相似 prompt → 历史产物（5-C） | 长期 | 生成前查（命中复用）；生成后写 |
| 反馈记忆 | MySQL `Feedback`（业务服务） | 1–10 评分 + 评论 | 长期 | 用户评分后写；既统计又回归 |

**检索闭环流程**：
```
[0] 用户需求(prompt)
 [1] 构造查询/关键词 ── A 原始 prompt(稠密 embedding) + B Planner 结构化 spec 关键词 + C 意图 + D 项目上下文(userId)
 [2] Qwen text-embedding
 [3] Chroma 检索 Top-K(混合召回): components + memory + cache:gen
 [4] 拼装增强 Prompt
 [5] Router 分发 Skill        (cache:gen 命中 ⇒ 直接复用产物,省 token)
 [6] generate_site 内层(Planner→Coder→Reviewer)
 [7] 生成产物 HTML ──→ COS 投递(preview_url)
 [8] 异步回写 memory   [9] 异步回写 cache:gen   [10] 用户评分(1–10) → Feedback(统计+回归)
 [11] 优质产物(Reviewer 通过 + 高评分)经运营审核 → 沉淀 components
 ⇒ 闭环:读前增强 → 写后沉淀 → 模型越用越准、越省 token(自我改进飞轮)
```

> **⚠ 未实装（闭环未打通）**：因 §9 的 RAG 未注入 `generate_site`，[3]/[4] 在生成链路中不发生；[8] memory 回写、[9] cache:gen 回写、[10] Feedback 均依赖未建的表 / 未接的写入，**当前均不执行**。记忆闭环目前仅停留在设计，未落地。

**关键词来源（混合检索）**：稠密向量负责语义召回，关键词与 metadata 负责精确过滤 + 排序加权。
- **A. 原始用户 prompt**：整句 embedding + 轻量分词去停用词得关键词（主体）。
- **B. Planner 结构化 spec 抽取**：最准的查询构造（推荐主来源）。
- **C. Router 意图标签**：限定检索 scope（只搜相关 collection）。
- **D. 项目 / 用户上下文**：`memory` 检索的过滤条件（`userId` 必带）。
- **推荐**：以 **B + A** 为主构造查询，**C** 做 scope 限定，**D** 做 `memory` 过滤（模型判定优先、规则仅辅助）。

---

## 11. COS 预览投递（A2）

生成的单文件 HTML **上传腾讯云 COS**，预览走 **COS 访问域名直链**（天然跨域隔离，见《前端细节》§7）。

- **已配置（实际值）**：
  - 存储桶：`seedhtml-1252059540`
  - 地域：`ap-guangzhou`
  - 访问域名：`https://seedhtml-1252059540.cos.ap-guangzhou.myqcloud.com`
  - 密钥：主账号暂代（生产前务必降 CAM 子账号并轮换，见 §14 E1）
  - 生命周期：无过期时间
- **代码**：`ai_service/app/preview_pusher.py`（用 `cos-python-sdk-v5`）上传到 `cos://{bucket}/{base_path}/{user_id}/{site_id}/{version}/index.html`，返回直链；生成完成（Reviewer 通过）后 Orchestrator 调 `preview_pusher` 投递，URL 写入 `Artifact.preview_url`（MySQL）与 SSE `done` 事件。
- **环境变量**：`COS_SECRET_ID` / `COS_SECRET_KEY` / `COS_BUCKET` / `COS_REGION` / `COS_PREVIEW_DOMAIN` / `COS_BASE_PATH=previews` / `COS_TTL_DAYS=0`。
- **降级**：COS 投递失败或离线 → 前端降级 `iframe srcdoc`。
- **安全**：同源隔离由独立域天然保证；`sandbox="allow-scripts"` 保留；CSP 待绑定自定义域 + CDN 时注入（决策 #32）。

---

## 12. 优化方案（1-C / 2-C / 3-C / 4-C / 5-C）

### 12.1 生成任务并发 → 1-C：Redis 队列 + Worker 池
- 生成请求经 **Redis `queue:generate` + Worker 池**（可多副本，水平扩、重启不丢）。流程：业务 SSE 到达 → 入 `queue:generate` → Worker 消费 → Router 分发 Skill → 进度（think/node/token）经 **Redis Stream** 写入 `gen:stream:<traceId>`（可回放，`after` 断点续传）→ SSE 端点订阅转发前端 → `done` 结束。并发上限 = Worker 数。

### 12.2 模型故障转移 → 2-C：跨模型降级路由
- `FallbackRouter` 按用户优先级 + plan 默认回退（§2）；`degraded: true` 标记透出前端「已降级」。默认序 HY3 → Qwen → DeepSeek。

### 12.3 产物可运行校验 → 3-C：静态分析 + LLM 自审
- Reviewer 两步：① 静态分析（HTML 解析，检查 doctype、标签闭合、script 语法粗筛、禁止危险 API 如 `eval` / 注入外部源的 `innerHTML`）；② LLM 多轮自审（Reviewer 建议 → Coder 修订 → 再评审，≤3 轮）。不引入 Playwright 重依赖，显著降白屏率。

### 12.4 预览安全隔离 → 4-C：独立沙箱域 + 严格 CSP
- 独立子域（COS 默认域天然不同源）+ `sandbox="allow-scripts"`（不含 `allow-same-origin`）+ 严格 CSP（当前未注入，待绑定自定义域 + CDN 时回源重写 `default-src 'none'` + 白名单 CDN / 字体）。

### 12.5 生成结果缓存 → 5-C：语义相似缓存
- Qwen text-embedding 对用户 prompt 向量化 → 查 Chroma `cache:gen` → 相似度 > 阈值直接复用 / 微调上次 HTML，省 token。用户可「强制重生成」覆盖。**依赖 M1 向量库**（MVP 暂不做，或以精确 prompt 哈希过渡）。**（⚠ 未实装：cache:gen 回写未接（§10 闭环未打通），generate_site 不查缓存）**

---

## 13. 资源层连接池（Chroma / Redis，归 AI 服务）

- **Redis（redis.asyncio 连接池）**：`pool = redis.asyncio.ConnectionPool.from_url(REDIS_URL, max_connections=50, decode_responses=True, health_check_interval=30)`；客户端全局复用。用途：`queue:generate` / `gen:progress:<traceId>` / `gen:inflight` / `cache:models` / `cancel:<traceId>`（C1）/ `cache:gen`。
  - **协议兼容（v0.4.4 修复）**：部分云 Redis（老版本/代理）不支持 redis-py 默认的 `HELLO`(RESP3) 握手，会直接 `unknown command HELLO` 导致 `/generate` 运行时 500。`queue.py` 的 `RedisBackend` 强制 `protocol=2` 避开 `HELLO`；并在构造时做一次**真实 PING 握手**探测，连不上或不兼容即回退 `MemoryBackend`（进程内 asyncio 队列，单进程开发足够），保证本地无 Redis 也能跑通闭环。`DEV_MEMORY_QUEUE=1` 或 `REDIS_URL=memory://` 可强制内存模式。
- **Chroma（单例 HttpClient + httpx 连接池）**：复用**单例 `HttpClient`**，注入自定义 `httpx.AsyncClient(limits=httpx.Limits(max_connections=50, max_keepalive_connections=20))` 复用 TCP 连接。启动时 `client.get_or_create_collection(name=...)` 确保集合存在。
- **统一约束**：客户端均在服务启动时初始化为模块级单例，关闭时释放；禁止请求内反复新建连接。

---

## 14. 生产密钥管理（E1，待生产落地）

- 当前本地 `.env` / `.env.production` 明文存密钥（gitignored），满足开发。
- 生产改为 **Docker Secrets**（Swarm / Compose `secrets:`）或等效密钥管理注入（腾讯云 SSM / 主机文件 `chmod 600`），密钥以文件挂载进容器（`/run/secrets/xxx`），业务代码读文件而非环境变量；绝不进镜像层 / 仓库。
- **COS 主账号风险**：当前用主账号，生产前务必降为仅此桶读写的 CAM 子账号并轮换主账号密钥（主账号泄露 = 全账号失陷）。
- `config.py` 增加「secrets 文件优先、环境变量兜底」读取链；新增 `docker-compose.prod.yml` 的 `secrets:` 段。

---

## 15. AI 服务 Redis Keys 约定

```text
queue:generate            生成任务队列(MVP 即上, 1-C)
gen:stream:<traceId>     生成进度流(think/node/token/done,可回放 XRANGE+XREAD,with after 断点续传)   ← 注:业务端《业务端细节》§14 同此名;本文档旧版误写作 gen:progress
gen:inflight             在途生成计数(限并发, 1-C)
cancel:<traceId>         级联取消信号(C1)
cache:models              模型列表缓存
cache:gen                prompt Embedding → 历史生成(5-C, 随 M1 向量库,当前未回写)
# 注: 业务服务侧 Redis Keys 见《业务端细节》§14

# 统计系统(STAT) — AI 核心写入, 业务服务读(同库 Redis)
an:orch:*               多意图编排统计(总次数/策略parallel|mixed/子任务数/成功率/耗时)
# 子键: an:orch:total / an:orch:strategy:{s} / an:orch:split_count(zset) / an:orch:success_rate(zset) / an:orch:duration_ms(zset)
an:subtask:*            子任务统计(per-skill/status/risk/耗时)
# 子键: an:subtask:total / an:subtask:{skill}:{status} / an:subtask:status:{s} / an:subtask:risk:{r} / an:subtask:duration_ms(zset)
an:generate:*           AI 核心总生成请求数(单意图+多意图, 独立于编排统计)
# 子键: an:generate:total
```

## 16. 本地运行日志（按日期落盘）
- **落盘位置**：`backend/ai_service/logs/ai_service.log`。与业务服务同款机制：按**自然日**滚动为 `ai_service.log.YYYY-MM-DD`，保留最近 **30 天**，控制台同步输出。
- **实现**：`app/logging_config.py` 的 `setup_logging("ai_service")`，在 `main.py` 引导注册（`bootstrap()`）与路由装配前调用，确保启动期与注册期日志也能落盘；幂等（reload 不叠加 handler）。
- **覆盖范围**：应用内各 logger（queue / router 等）+ uvicorn 访问/错误日志。
- **日志目录** `backend/ai_service/logs/` 已加入 `.gitignore`，不入库。

## 17. 7-Agent 矩阵（v0.7.0+）

| Agent ID | 头像 | 名称 | 职责 | 触发意图 |
|----------|------|------|------|----------|
| explain | 🤖 | 智能小胡 | 问答/搜索/闲聊 | learn/* |
| requirement_agent | 📋 | 需求小胡 | 多轮需求对话→需求文档 | build + status∈{draft,planning} |
| builder_agent | ⚡ | 建站小胡 | 基于需求文档生成HTML | build + status∈{planned,building,built} |
| design_agent | 🎨 | 设计小胡 | 配色/布局/字体 | learn/design |
| review_agent | 🔍 | 评审小胡 | 性能/SEO/可访问性 | code/refactor |
| fix_agent | 🔧 | 修复小胡 | Bug诊断+代码修复 | code/fix |
| generate_doc | 📄 | 文档小胡 | README/教程 | doc/* |
| write_code | 💻 | 编码小胡 | 函数片段/UI组件 | code/snippet, code/component |

Agent 注册表通过 `/agents` 端点（匿名）下发，前端启动时 fetch 缓存。

## 18. 上下文关联（v0.7.0+）

三层上下文的发送格式：`context_hint`(前端WebLLM检测) > Chroma向量检索(后端降级) > 无。

- 前端 WebLLM `contextCheck()`：将当前消息+最近12条对话发给本地 Llama-3.2-1B，返回一句话上下文摘要。
- 后端 Chroma `find_relevant_messages()`：向量相似度 > 0.55 的保留。
- 意图分类器接收 `context_hint` 参数拼入 prompt。

## 19. Projects 状态机（v0.7.0+）

```
draft → planning → planned → building → built
```

| 字段 | 类型 | 说明 |
|------|------|------|
| status | VARCHAR(20) | 建站状态 |
| requirement_doc | TEXT | 需求文档 JSON |

---

## 20. v0.8.5 后置 QC 三裁判（M1）

> 详细实现见 [v0.8.5-质检复盘落地实现.md](./v0.8.5-质检复盘落地实现.md)。

### 20.1 模块 `qc.py`
- **默认开启、默认三裁判全量**：`deepseek / qwen / hy3`（`QC_JUDGES`），并行非流式 `ainvoke` 各按 6 维打分。
- **6 维度**（`QC_DIMENSIONS`）：`correctness / completeness / compliance / efficiency / readability / safety`，各 1-10。
- **聚合**（`_aggregate`）：每维 `mean`（有效分均值）、`variance`、`scores:[deepseek,qwen,hy3]`（0=未参与/失败）；`overall`=6 维 mean 平均；`needs_review`=最大方差≥4 或安全高危；`partial`=有裁判失败。
- **混合判定（降本）**：`compliance/safety/efficiency` 叠加 `run_safety` 确定性地板（零成本）；`correctness/completeness/readability` 走 LLM 三裁判。

### 20.2 注入点（`core/queue.py` 正常路径）
- 累积 `token` 事件为 `assistant_text`；**拦截 `done`** 暂不发。
- 调 `run_safety(messages, project_constraints).risk_level` 得 `safety_risk`。
- `asyncio.wait_for(run_qc(...), 60s)` → 先 `publish qc` 再 `publish done`。
- QC 失败仅告警、绝不阻塞 `done`；`block/unsupported/confirm/options/error` 路径不跑 QC。

### 20.3 事件协议
- 新增 SSE 事件 `qc`（`TERMINAL_EVENTS` 不含它；业务代理透传未知事件无需改转发逻辑，仅 `finally` 落库时捕获）。
- 前端 `chat.ts` 监听 `qc` → `onQc`；气泡展示 QC 徽标，管理后台聚合绘图。

---

## 21. AI 核心编排统计（analytics.py，v0.8.x 实装）

> 与《业务端细节》§18 的 STAT 协同：AI 核心(7102)**写** `an:orch:*` / `an:subtask:*`，业务(7101)**读**同库 Redis 聚合。

### 21.1 写入（queue.py 正常多意图路径）
- `core/queue.py` 在 split 分支：`t0_split=time.time()`，建 `sub_start`(各子任务开始时刻) 与 `sub_meta`(id→(skill,risk)) 映射。
- 监听编排事件：`subtask_start`→记开始时刻；`subtask_done`→算耗时 `record_sub_task(skill,'done',risk,dur_ms)`；`subtask_fail`→按 reason 映射 status(blocked/skipped/failed) 后 `record_sub_task(...)`；`merge`→缓存合并数据。
- 循环结束后：`rate=sc/(sc+fc)`、`dur_ms`，调 `record_orchestration(len(sub_tasks), 'mixed' if has_dep else 'parallel', dur_ms, rate)`。

### 21.2 聚合（analytics.py）
- `record_orchestration(split_count, strategy, duration_ms, success_rate)`：`an:orch:total` + `:strategy:{s}` 计数 + `split_count`/`success_rate`/`duration_ms` 写 zset（供 p 分位）。
- `record_sub_task(skill, status, risk_level, duration_ms)`：`an:subtask:total` + `:{skill}:{status}` + `:status:{s}` + `:risk:{r}` 计数 + `duration_ms` zset。
- `record_generate_request()`：`an:generate:total` 计数 +1，记录 AI 核心收到的**总生成请求数**(含单意图+多意图)，独立于编排统计、反映 AI 核心真实负载；业务 `analytics_snapshot` 的 `orchestration` 块以 `ai_core_requests` 字段呈现。
- `orchestration_stats()`：读上述键聚合出 总次数 / 策略分布 / 子任务数·成功率·耗时百分位数 / 子任务 status_dist·risk_dist·per_skill·耗时 p50-p99。
- 惰性连接：模块级 `_redis_client` 经 `aioredis.from_url(settings.redis_url, socket_connect_timeout=3, socket_timeout=3)` 缓存；连接失败降级为 `False` 静默跳过，不影响主生成链路。

## 近期变更（v0.9.0 · 2026-07-23）

### Chroma 六集合索引（`knowledge/chroma.py`）
- 修复 `_client()` HttpClient 构造 bug（chromadb 1.5.9 不支持 base_url→改用 host/port）
- 新增通用工具：`_retrieve_where`（metadata 过滤）/ `_upsert`（幂等写入）/ `_short_hash`
- 六集合 API：`conversation_context` / `user_preferences` / `project_memory` / `project_code` / `error_patterns` / `components`
- 20 条 HTML/CSS 常见错误种子数据（`seed_error_patterns`）
- 全部按 `project_id`/`user_id` metadata where 隔离

### AI 核心流程升级
- **上下文深度融合**（Phase A）：`intent/context.py` 新增 Chroma 用户偏好 + 项目记忆检索，`ContextResult.chroma_context` 字段
- **阶段自评**（Phase B）：`generate_site.py` SYS_REVIEWER 扩展为 6 维（scores+issues），`_review` 返回结构化自评
- **修复闭环**（Phase C）：新建 `core/repair.py`——RepairState Redis 持久化（10min TTL）、`locate_failed_stage`（自评×QC 交叉定位）、`repair_loop`（≤3 轮 / 30s / best-of-N）、兼容 Stream 续跑
- **闲聊重答**（Phase D）：explain 低分→1 轮轻量重答

### done 钩子链
```
done 事件 → QC 三裁判 → 闲聊重答(explain低分) → L2 对话精炼(refined事件)
→ L2+ 蒸馏(写Chroma偏好+项目记忆) → P4 代码索引(project_code) → §8 git提交
```

### 统计（`analytics.py`）
- 新增：`record_repair` / `record_distill` / `record_code_index` / `record_refine` / `record_chat_retry`
- Redis `an:v090:*` 前缀，业务端 `analytics_snapshot` 可读取

### 修复的预埋 Bug
- `_client()` HttpClient(base_url=...) 从未正常工作 → 修复前向量检索一直回退到关键词兜底

## 历史变更（v0.8.6 · 2026-07-22）
- **P0-1 事件循环非阻塞化**：`skills/*` 内 `chat.invoke()` / Chroma `index_message` 等同步重操作已用 `asyncio.to_thread(...)` 包裹（`generate_site.py` 等），避免单个生成任务独占事件循环、并发天花板≈1 的老问题；与 §8 的 `asyncio.to_thread(commit_site_for_trace, ...)` 一致（git 调用本就同步，必须离线线程跑）。
- **§8 站点 Git 版本控制（`core/git_site.py`）**：每 trace 一个仓库 `ARTIFACT_DIR/anon/<trace_id>`；`ensure_repo`（init + user 配置 + LFS 开启并 track 图片/字体等二进制）；`commit`（有改动才提交，message 带 skill+prompt 摘要）；`list_versions` / `rollback`（checkout 后线性提交，不 `reset --hard`）/ `create_branch` / `bundle_to_cos`（失败 `skipped=True` 优雅跳过）/ `restore_from_cos`。Worker 在 `done` 事件发布后调用 `_commit_after_done`，对 `generate_site`/`write_code`/`orchestrator` 三类 skill 自动提交一次版本（本地优先；COS 上传 best-effort）。
- **安全加固（`intent/common.py`）**：`SAFETY_HARD_KEYWORDS` 增补「sql 注入 / 拖库 / 脱库 / 撞库 / 导出用户数据 / 窃取数据 / 水军 / 刷量 / 刷单 / 刷评论 / 批量注册 / 养号 / 打码平台 / 接码平台 / 钓鱼 / 木马 / 病毒 / 后门 / 勒索 / 暴力破解 / ddos / 拒绝服务攻击」等；命中即 critical 拦截（建站/学习意图亦不豁免）。修复此前「sql 注入拖库」「刷水军」被判为中性可执行的漏判。
- **Bug 修复**：`generate_site.py` 的 `_review` 由同步改为 `async def`（原 P0-1 误把 `await asyncio.to_thread(_chat, ...)` 写进同步函数，导致 ai_service 整体 `SyntaxError` 导入失败），3 处调用点补 `await`，ai_service 现已正常启动。

