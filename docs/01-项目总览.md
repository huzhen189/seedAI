# AI 生成器平台 — 项目总览

> 本文档是平台的总览规格，定义**产品定位、整体架构、技术选型、路线图与部署**，并汇总关键设计决策。
> 分层详细设计见三份配套文档（当前方案的权威细节来源）：
> - 《前端细节》—— 前端（Vue 3 + Vite + TS）的实时流、WebLLM、预览隔离、管理后台。
> - 《业务端细节》—— 业务服务（FastAPI）的鉴权、用户系统、配额、SSE 代理、Trace 回放、指标、数据模型。
> - 《AI 核心端细节》—— AI 服务（FastAPI）的模型抽象层、Router+Skill 编排、generate_site 多 agent、RAG、记忆闭环、工具注册表、COS 投递。
>
> 本文档集替代原单一历史文档 `AI-Generator-开发文档-v1.5.md`（该文件保留仅作历史参考，不再维护）。

---

## 1. 产品定位与核心价值

### 1.1 产品定位
- **形态**：类 v0.dev / bolt.new / Lovable 的「对话即生成」平台。
- **使用范围**：个人自用为主 + 小范围分享；用户系统支持账号密码（短信 / 收费预留接口）。
- **首要产物**：网站 / 前端页面（带实时预览、可分享、可迭代）。
- **后续扩展**：文档生成、代码工程生成（架构预留，不在 MVP）。

### 1.2 核心价值
自然语言描述需求 → 业务服务编排 → 核心 AI 服务（多 agent + RAG）生成可运行网页 → 实时预览 → 对话迭代 → 分享 / 导出。

---

## 2. 功能范围

### 2.1 MVP（第一阶段必须有）
| 模块 | 归属服务 |
|------|---------|
| 用户系统（账号密码） | 业务服务 |
| 对话生成（流式） | 业务服务入口 → AI 服务生成 |
| 多模型自选 | 业务服务透传 modelId → AI 服务 |
| 多 Agent 编排 | 核心 AI 服务 |
| 实时预览 | 前端（iframe / 沙箱） |
| AI 思考过程可视化 | 前端（思考链路面板，SSE 实时展示 Router/Skill 思考链路） |
| 迭代修改 | 业务服务 + AI 服务 |
| RAG 增强 | 核心 AI 服务 |
| 用量限流（plan/tier） | 业务服务 |
| 分享链接 | 业务服务 |

### 2.2 第二阶段（增强）
版本历史与回滚 / 代码在线编辑 / 导出 zip / 一键部署 / 模板库运营 / 收费模式接入。

### 2.3 暂不做（已明确后置）
- **手机短信验证码登录**：仅保留 `SMSService` 抽象层与 Redis 验证码结构；开放时再接真实服务商。
- **完整订阅计费与支付**：只做 plan/tier 配额抽象与表预留位；未来接微信 / 支付宝 / Stripe 只增表、改配额取数一处。
- 复杂后端逻辑生成 / 团队协作权限体系。

---

## 3. 技术选型总览

| 层 | 选型 | 说明 |
|----|------|------|
| 前端 | **Vue 3 + Vite + TypeScript**（纯静态 SPA） | 对话 UI、预览面板、模型选择器、登录页、`/admin` 管理台 |
| 前端 UI | Tailwind CSS + Naive UI / Element Plus | 快速搭建、风格统一；浅色简洁主题 |
| 业务服务 | **FastAPI (Python)** | 鉴权 / JWT、用户、项目、消息、分享、配额限流、用量计量；唯一对外入口 |
| 核心 AI 服务 | **FastAPI (Python)** | 模型抽象层、Router+Skill 路由、generate_site 内层 LangGraph 轻量多 Agent、RAG；独享模型 Key |
| Agent 编排 | **Router+Skill（外层）+ LangGraph（内层）** | 外层 Router 意图分发 / 鉴权 / 配额；内层仅 generate_site 走 LangGraph 状态图 + 条件边（评审回退）；其余 skill 单次 LLM |
| 认证 | **JWT(Access) + Refresh 存 Redis**；密码 bcrypt/argon2 | 无状态令牌利于横向扩容 |
| 短信 | **SMSService 抽象层（预留，先不接）** | 同模型抽象层思路；开放时再接真实服务商 |
| AI 编排 | OpenAI 兼容 SDK（Python）/ 自封装流式 | 统一流式输出（async generator） |
| 模型 | **DeepSeek + Qwen + HY3**（前端可选，可扩展） | 模型抽象层，见《AI 核心端细节》 |
| 关系库 | **MySQL** | 业务数据（用户 / 项目 / 对话 / 产物），归业务服务 |
| 向量库 | **Chroma** | 组件 / 记忆检索，归 AI 服务 |
| 缓存 / 队列 | **Redis** | 会话 / 限流（业务）、队列 / 缓存（AI） |
| ORM | **SQLAlchemy(async)** | 与 FastAPI / AI 服务一致 |
| 可观测性 | **自研轻量 /metrics + Stats 聚合 + SSE**（MVP）；Prometheus+Grafana（可选升级） | 见《业务端细节》管理监控后台 |
| 控制面 | **Orchestrator 抽象**：DockerCompose（MVP）/ K8s（生产） | 启停 / 扩缩容 |
| 部署 | **Docker Compose（本地）→ 多服务器扩容** | 见 §8 |

> **前后端跨语言说明**：前端 TS、后端 Python（双服务），通过 HTTP/SSE 解耦。接口契约以 **OpenAPI(Swagger)** 为准；本地 Vite dev 配 `server.proxy` 将 `/api/*` 代理到业务服务（`http://seedapi.huzhen.net.cn:8000`），业务服务再内网调用 AI 服务（`localhost:8001`）。模型 Key 与短信 Key 只在 AI 服务 / 配置内，前端与业务服务均不接触。

---

## 4. 系统架构（微服务）

后端拆为两个独立部署的服务，职责清晰、可独立扩缩容：

```
┌──────────────────────────────────────────────────────────────┐
│  前端层  Vue 3 (TS)   对话 UI · 预览 · 登录 · 模型选择器 · /admin    │
└───────────────────────────────┬──────────────────────────────┘
                                 │ 对外 HTTPS
┌───────────────────────────────▼──────────────────────────────┐
│  业务需求服务器 (Business Service)  ← 唯一对外入口               │
│  鉴权/JWT · 用户 · 项目 · 消息 · 分享 · 配额限流 · 用量计量       │
│  MySQL(业务数据) · Redis(会话/限流)                             │
└───────────────────────────────┬──────────────────────────────┘
              内网 HTTP/SSE(同步转发流式) │
┌───────────────────────────────▼──────────────────────────────┐
│  核心 AI 服务器 (AI Service)  ← 仅内网,持有模型 Key             │
│  模型抽象层(Provider) · Router+Skill · LangGraph(generate_site) · RAG  │
│  Chroma(向量) · Redis(队列/缓存) · → DeepSeek/Qwen/HY3          │
└───────────────────────────────────────────────────────────────┘
```

**关键设计**
- **业务需求服务器**：唯一对外暴露的入口。负责鉴权、用户、项目管理、对话消息、分享链接、配额限流、用量计量。**不持有模型 API Key**。生成类请求转发给 AI 服务。
- **核心 AI 服务器**：仅内网可达（网络隔离 / 不暴露公网）。负责模型抽象层（Provider 注册表）、Router+Skill 路由、generate_site 内层 LangGraph 轻量多 Agent、RAG（Chroma）、流式生成。**独享模型 API Key**。
- **内部信任**：AI 服务不重复校验终端用户，信任来自业务服务的内网调用（可加内部 service token 或纯网络隔离）；**前端永不可直达 AI 服务**。
- **数据与 Redis 归属**：MySQL 归业务服务；Chroma 归 AI 服务；Redis 可共享实例（业务用会话 / 限流，AI 用队列 / 缓存），后期可按服务拆分实例。

---

## 5. 服务间通信（SSE 全链路）

- **前端 ↔ 业务服务 = SSE**（每问一连接，结束断连）：前端 `EventSource` 连 `GET /api/chat`，事件协议见《前端细节》§3；取消用 `AbortController`（前端）+ 服务端级联取消（业务 → AI）。
- **业务服务 → AI 服务 = SSE 透传**：业务 `httpx` 流式读取 AI 服务 `/generate` 的 SSE 事件，原样转发给前端。**全链路不使用 WebSocket**——前端实时生成流一律走 SSE。
- **级联取消（C1）**：前端断开 → 业务感知连接关闭 → 经 Redis 广播 `cancel:<traceId>` → AI Worker 中断当前生成，释放槽位、停止计费。

服务间调用关系总览（细节见《AI 核心端细节》§3）：

```
前端 → 业务服务 /api/chat (SSE, 校验 JWT cookie, 记用量/配额)
         └─ [WebLLM 旁路] 若选「本地 WebLLM」:前端本地首过, 同构 think/node/token 事件, 失败/超时回退下方
         └─ 内网 → AI 服务 /generate (SSE, 经 Redis 队列+Worker 产出)
                     └─ Router(意图/鉴权/配额) → 分发 Skill
                           ├─ generate_site → {Planner→Coder→Reviewer}(LangGraph, 轻量多 Agent)
                           ├─ write_code / generate_doc / explain → 单次 LLM
                           ├─ rag_retrieve → Chroma(语义缓存)
                           └─ 模型抽象层 + FallbackRouter → DeepSeek/Qwen/HY3(降级)
         └─ think/node/token 事件经业务服务透传回前端 → 思考面板 + iframe 预览
```

---

## 6. 本地开发环境（域名 / hosts / 代理）

### 6.1 三套域名（本地开发映射）
| 用途 | 域名 | 本地指向 | 生产指向 |
|---|---|---|---|
| 前端主站 | `seedai.huzhen.net.cn` | `127.0.0.1`（本机 Vite dev :3000） | 真实服务器 / CDN / 静态托管 |
| 后端 API | `seedapi.huzhen.net.cn` | `127.0.0.1`（本机 business-api :8000） | 真实服务器（公网入口） |
| 预览沙箱 | COS 默认域 `seedhtml-1252059540.cos.ap-guangzhou.myqcloud.com` | 不代理（走线上 COS） | 腾讯云 COS（已配） |

- **为什么改 hosts**：本地无 DNS，用 hosts 把「前端主站 + 后端 API」两域名指到 `127.0.0.1`，使本地联调时前端 / 后端用**与生产一致的域名**（CORS、`cookie_domain`、Cookie 作用域、跨域隔离逻辑都能按真实环境验证），避免 `localhost` 与域名混用导致的环境差异 bug。
- **预览域不配本地 host**：预览产物存于线上腾讯云 COS，本地 / 线上均直接访问其真实默认域名 `seedhtml-1252059540.cos.ap-guangzhou.myqcloud.com`，天然跨域隔离，无需本地解析。

### 6.2 hosts 配置（本机 `C:\Windows\System32\drivers\etc\hosts`）
```
# seedAI 本地开发域名(仅前端主站 + 后端 API 需本地解析;预览域走线上 COS 默认域,不在此列)
127.0.0.1 seedai.huzhen.net.cn
127.0.0.1 seedapi.huzhen.net.cn
```
- 编辑需管理员权限；保存即生效，必要时 `ipconfig /flushdns` 清缓存；改前先备份 `hosts.bak`。
- 验证：`ping seedai.huzhen.net.cn` / `ping seedapi.huzhen.net.cn` 回 `127.0.0.1` 即正确。

### 6.3 配合项
- 前端 `vite.config.ts` 的 `server.proxy` 将 `/api` 代理到 `http://seedapi.huzhen.net.cn:8000`，且 dev `server.port = 3000` 对齐 hosts/CORS；与 `CORS_ORIGINS` 的 `http://seedai.huzhen.net.cn:3000` 完全一致，消除跨域源不一致。
- `COOKIE_DOMAIN` 本地留空（浏览器按宿主 `seedai.huzhen.net.cn` 自动作用）；`CORS_ORIGINS` 含 `http://seedai.huzhen.net.cn:3000`。
- 预览域：直接走线上 COS 默认域直链（见《AI 核心端细节》§11），前端 `iframe` 的 `src` 即该线上 URL。

---

## 7. 开发路线图

- **M0 打通闭环**：搭双服务骨架 —— 业务服务（鉴权入口 + SSE `/api/chat` 代理 + 结构化日志 + Redis `queue:generate` 透传）+ AI 服务（Orchestrator 编排 Planner→Coder→Reviewer + 思考链路事件 + 模型抽象层，经 Redis 队列 + Worker 产出）+ 前端 **WebLLM 首过旁路** → DeepSeek/Qwen/HY3 流式生成单文件 HTML + 思考过程 → 业务服务经 SSE 转回前端（思考面板 + iframe 预览：产物上传 COS 走独立域直链，失败降级 `srcdoc`）→ 提问结束断连。**M0 匿名跑通**（不做登录，用临时匿名 `user_id`）。Docker Compose 起 business-api / ai-service / mysql / redis / chroma。
- **M1 可用**：业务服务补全用户系统（账号密码 + JWT）→ 配额限流（plan/tier，Redis）→ 缓存读写（Cache-Aside）+ 项目 / 对话持久化（MySQL）→ AI 服务基础 RAG（components 检索）。
- **M2 可分享 + 记忆**：分享只读链接 + memory 回写与检索 + 异步写队列 + 错误队列定时检查器 + 生成队列 / Worker + 迭代修改。
- **M3 增强**：手机短信登录、Designer 深度美化、Sandpack 多文件、版本历史、导出 zip、模板库运营、收费模式接入、多服务器扩容上线。
- **M4 运维监控后台**：RBAC 管理页 → 实时监控（各服务状态 / 性能 / 接口 / 访问量 / 模型用量，SSE 推送）→ 手动启停 / 扩缩容（Orchestrator）→ 统计系统仪表盘。

---

## 8. 部署方案

**阶段一 — 本地（Docker Compose）**
`docker-compose.yml` 起全套（各自独立容器）：
- `frontend`（Vue 3 + Vite + TS，静态 SPA；docker 镜像用 Vite build 产物 + 轻量静态服务器，如 nginx-alpine）
- `business-api`（业务服务：FastAPI，持 DB/JWT 配置；含缓存读写与错误队列定时检查器，MVP 内置）
- `ai-service`（AI 服务：FastAPI + Router+Skill + LangGraph（generate_site 内层），持模型 Key / Chroma 配置）
- `mysql`、`chroma`、`redis`

**阶段二 — 多服务器扩容**
- 前端、业务服务、AI 服务各自镜像化，可分别多实例部署。AI 服务算力吃紧时**单独加副本**（微服务拆分的核心收益）。
- 数据层独立部署或云托管。前置 Nginx / 负载均衡：仅暴露前端与业务服务，AI 服务留在内网。
- 会话 / 限流 / Refresh Token 在 Redis，保证业务服务无状态、可水平扩展；AI 服务亦无状态（状态在 Chroma/Redis），可水平扩展。
- 长耗时生成走 Redis 队列 + Worker 池（MVP 即上，挂在 AI 服务侧，可多副本水平扩展）；错误队列的定时检查器随业务服务内后台任务。

---

## 9. 关键设计决策汇总（当前方案）

下表为当前方案的决策登记（已去除历史修订记录，仅保留结论）。详细依据见各配套文档。

| # | 主题 | 当前方案 |
|---|------|----------|
| 1 | JWT 细节 | Access 15min + **HttpOnly Cookie** 防 XSS；Refresh 存 Redis（7 天，可吊销） |
| 2 | ORM | **SQLAlchemy(async)** |
| 3 | Embedding 模型 | **Qwen text-embedding**（M1 落地） |
| 4 | HY3 API/鉴权 | **OpenAI 兼容**：`base_url=https://tokenhub.tencentmaas.com/v1`，model=`hy3`，Key=`TokenHub_QCSLinkedRoleInInitialization` |
| 5 | 视觉风格 | **浅色简洁** |
| 6 | 月度预算 | **不设硬预算**；free plan 每日生成配额默认 **50 次/天** |
| 7 | 提示词基调 | **通用中性** |
| 8 | 前端 ↔ 业务 传输 | **SSE**（每问一连接，结束断连） |
| 9 | 是否独立网关 | **业务服务兼聚合入口**，MVP 不单开网关 |
| 10 | 缓存写策略 | 写后**更新** Redis；同步写 = 注册/改密/订阅，异步写 = Message/UsageLog/Artifact/memory |
| 11 | 定时检查器 | **业务服务内后台任务**（MVP）；M2 可抽独立 Worker；重试指数退避 ≤5 次 → DLQ |
| 12 | 思考链路持久化 | **MVP 起即落 MySQL 可回放**（Trace / TraceEvent），不再「MVP 不落库」 |
| 13 | 业务 ↔ AI 传输 | **SSE 端到端**：业务 `httpx` 流式读 AI `/generate` SSE 透传 |
| 14 | 生成任务并发 | **Redis 队列 + Worker 池**：SSE 入 `queue:generate`、Worker 消费、进度经 Redis 转发；可水平扩、重启不丢 |
| 15 | 模型故障转移 | **跨模型降级路由（FallbackRouter）**：预算 / 套餐选主模型 + 用户前端手选备用优先级，失败按序回退；前端显示「已降级」 |
| 16 | 产物可运行校验 | **静态分析 + LLM 自审**：HTML 解析查错 + Reviewer 多轮自修（复用 LangGraph 回退） |
| 17 | 预览安全隔离 | **独立沙箱域 + 严格 CSP**：iframe 独立子域 + `sandbox` 属性 + CSP |
| 18 | 生成结果缓存 | **语义相似缓存（5-C）**：Embedding + Chroma 复用；随 M1 向量库落地 |
| 19 | 权限分级（RBAC） | **三级**：`super_admin`（全部）/ `admin`（仅查看后台）/ `user`（普通用户）；控制面 super_admin-only；初始 super_admin 经 `SEED_SUPER_ADMIN` 注入 |
| 20 | 编排架构 | **Hybrid**：Router+Skill 外层分发（意图 / 鉴权 / 配额）+ generate_site 内层轻量多 Agent（Planner→Coder→Reviewer，共享上下文，Designer 降为 style 约束）；其余 Skill 单次 LLM 直出 |
| 21 | 架构演进候选 | 内层 generate_site 升级为 **Plan-and-Execute + Reflexion 叠加**，外层 Router+Skill 不变 |
| 22 | 预览域投递 | **腾讯云 COS 对象存储 + 独立域直链**：桶 `seedhtml-1252059540`，`ap-guangzhou`；iframe 跨域隔离天然满足；含 CAM 子账号降权（主账号暂代）+ CSP |
| 23 | WebLLM 体验 | **单固定小模型 + 预取缓存**：锁定 Qwen2.5-7B 档，首屏空闲预取权重到 Cache Storage |
| 24 | SSE 取消 | **客户端 abort + 服务端级联取消**：`AbortController` → Redis `cancel:<traceId>` → AI Worker 中断 |
| 25 | 多租户 Key 账本 | **次数配额 + 全局限频 + 成本账本**：复用每日配额 + 令牌桶限频 + `UsageLog`（user×provider×model）成本归集 |
| 26 | 生产密钥管理 | **Docker Secrets 注入**：生产密钥以文件挂载进容器，不进镜像 / 仓库 |
| 27 | Schema 自动初始化 | **启动时自检建表（幂等）**：MySQL `create_all`（缺失才建）/ Chroma `get_or_create_collection` / Redis `ping` 探活 |
| 28 | 连接池管理 | **三库单例 + 连接池**：MySQL `pool_size=10,max_overflow=20`；Redis `max_connections=50`；Chroma 单例 HttpClient + httpx `Limits(50)` |
| 29 | 本地三域名 + hosts | `seedai` / `seedapi` 两行 hosts；预览走线上 COS 默认域不代理；前端 `vite.config.ts` 代理 `seedapi`，CORS/`cookie_domain` 一致 |
| 30 | M0 范围 | **M0 匿名跑通闭环**：M0 不做登录，`User`/配额/`UsageLog` 延到 M1；COS 路径用临时匿名 `user_id` |
| 31 | 模型默认降级序 | **HY3 → Qwen → DeepSeek**（用户指定可覆盖） |
| 32 | COS 预览 CSP | 当前未开 CDN，**仅 `sandbox` 隔离不打 CSP 响应头**；待绑定 `seedhtml.huzhen.net.cn` + CDN 后由回源重写注入严格 CSP |
| 33 | 多维度运营指标 | **6+1 类**：AI 效率 / 准确性 / 消耗量 / 回滚率 / 用户回馈 / 健康度 + RAG 检索质量；数据源 Trace/UsageLog/Feedback+Redis，`/admin` 看板呈现 |
| 34 | 对话回放存储 | **MySQL 双表 + Feedback**：`Trace` + `TraceEvent` + `Feedback` 落业务服务 MySQL，SQL 直查回放且与指标同源 |
| 35 | 产物版本方案 | **线性递增（回滚即复制为新版本）**：`Artifact.version` 整数自增，「恢复此版本」= 把选中旧版复制成最新版（历史不丢） |
| 36 | 对话评价 + 回归 | 每轮（Trace）结束后 **1–10 分评分 + 点赞/点踩 + 可选评论**；评分经 `trace_id` 关联完整上下文，可导出为模型回归 / 评测数据集 |
| 37 | RAG 命中率 | **方案2 阈值命中率 + 采纳率**：检索命中率（最高相似度 ≥ 阈值 0.7 占比）、无结果率、上下文采纳率；数据源 `TraceEvent(rag_retrieve)` |
| 38 | Skill/Agent 引入机制 | **SkillRegistry 注册表 + 开闭原则**：Router 按 `intent_tags` 匹配；新 Skill = 注册 handler；新多 Agent = 注册 `is_graph=True` 的 LangGraph app；M2+ 扫描 `skills/` 目录热插拔 |
| 39 | 记忆系统 + 关键词来源 | **多层记忆闭环** = 短期 `Trace` / 长期 Chroma `memory` / 组件 `components` / 语义缓存 `cache:gen` / 反馈 `Feedback`；**关键词 = 混合检索**，主来源 A 原始 prompt + B Planner spec，辅以 C Router 意图 + D userId/projectId |
| 40 | 前端实时传输 | **SSE（不使用 WebSocket）**：每问一连接、结束断连；业务 `httpx` 透传；取消用 `AbortController` + 级联取消 |
| 41 | 前端技术栈 | **Vue 3 + Vite + TypeScript**（纯静态 SPA）；状态 Pinia、UI Naive UI / Element Plus、浅色主题；`vite.config.ts` 代理 + `/admin` 路由 |
| 42 | 工具（Tool）来源机制 | **ToolRegistry 注册表 + 开闭原则**：Skill = 完整任务能力，Tool = agent 经 function calling 调用的原子操作；来源 = 内置 `@tool` 注册 / M2+ `tools/` 目录扫描 / M3+ 第三方 MCP；`rag_retrieve` 双入口（对外 Skill + 对内 Tool） |
| 43 | 本地 Skills/Tools 落地 | `backend/ai_service/app/` 下 `registry/` + `tools/` + `skills/` + `router.py` + `registries.py` 实现契约；**5 Skill + 9 内置 Tool**（成熟库），重依赖函数内懒加载、缺包优雅报错；`bootstrap()` 全量注册 + 目录扫描热插拔；新增 `/skills` `/tools` `/registry` 调试端点 |

> HY3 正式 Key = `TokenHub_QCSLinkedRoleInInitialization`（已写入 `.env` 的 `HY3_API_KEY`，不进仓库）。
> 监控指标栈 = 自研轻量（MVP）；管理后台 = Vue `/admin` 路由；控制面 = docker-compose 封装。
> 已后置（无需现在决定）：短信服务商（M3 接真实服务商时定）、收费模式表结构（只留 plan 字段 + 配额抽象，接支付时再建）。
