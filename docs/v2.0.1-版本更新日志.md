# v2.0.1 版本更新日志（2026-07-27）

> 本轮落地 `OPTIMIZE_PLAN.md` 第 1/2/3 节（命名统一 / 意图召回 / 删除体系），并响应"qwen 接口地址切到 token-plan"即时指令。第 4 节（B-轻角色重构）仅出方案、未实现。

## 变更总览

| 项 | 改动面 | 验证 |
|---|---|---|
| **qwen 地址切 token-plan** | `shared/config.py` 默认、`/root/.env` 的 `QWEN_BASE_URL`、`.env` 的 `QWEN_EMBEDDING_KEY`（token-plan key）、`rag_retrieve.py` 由硬编码 dashscope 改为 `settings.qwen_base_url`；清理 `qwen_api_host`/`QWEN_API_HOST`/`QWEN_DASHSCOPE_URL` | py_compile |
| **命名统一 Project.title→name** | `shared/models.py` 列改名 + 保留只读 `title` 属性；`business_repos.py` 删 name→title hack | 离线验证列映射 + py_compile |
| **意图召回 #2** | `rules_catalog.json` 增 `r_requirement_doc` 强规则；`cascade.py` 新增 [3.5] 强规则直路由；`multi_intent.py` 多意图门控放宽（连词强触发） | 离线规则/gate 验证 + `scripts/multi_intent_regression.py`（24 项全过） |
| **删除体系 #3** | `agent_delete.py` 四类边界判定表（block/high/medium）+ `confirm` 事件带 `risk_level`/`target`；`queue.py` confirm 透传 risk_level；前端 `ChatView.vue` 弹窗红/黄徽标 | 离线边界验证 + vue-tsc |

## 关键修复
- 修复 10 号"写/文档"此前可能漏召回 `requirement`：强规则白名单 + [3.5] 直路由，不再依赖向量/LLM 终判误判。
- 修复 13/14/19 多意图漏编排：并列连词直接强触发混合分层（不再要求命中 ≥2 意图大类）。
- 修复 `rag_retrieve.py` 硬编码 dashscope 地址与 `.env` 的 token-plan key 不匹配（RAG embeddings 此前可能失败）。

## 约束与待办
- **Project 改列需重置数据库重写**（`scripts/reset_all.py` DROP + `init_db()`）；由用户委派手动执行，Agent 不自动拉起重启。
- 诗歌/摘要（3/6）按用户判定**不算 bug**：其关键词不在 doc 白名单，不走 requirement。
- 前端 `confirm` 弹窗按 `risk_level` 渲染（high=红 / medium=黄）。

## 测试
- `scripts/multi_intent_regression.py`：§2.1 强规则白名单 + §2.2 连词门控 + T1~T9 方案 A/B 路由，全部通过（EXIT 0）。
- `_e2e_harness.py`：更新 #3/#6/#10/#13/#14/#18/#19 的 `expect` 注释（与 OPTIMIZE_PLAN 对齐），待重置后回归。
- 前端 `vue-tsc --noEmit` 通过。

> 提交：`git commit`（本地 + tag，不 push）。
