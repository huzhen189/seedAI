# 后端架构优化方案：统一 Role Pipeline，消除"两套逻辑并行"

> 文档状态：**方案稿（未改代码）**，等你确认后落地。
> 关联文档：`多意图确认续跑改进方案.md`（G1–G8，是在"不重构"前提下的 bug 修复；本文是更上层的架构收敛）。

---

## 一、问题本质：不是"两套引擎"，是"一个引擎被两层规划逻辑包裹"

你的直觉完全正确——确实存在混乱，但准确说是：**执行引擎只有 1 套，但"规划/编排逻辑"有 2 套在并行且互相重叠**。

### 1.1 分叉点不在入口，在 Worker（这是观感混乱的第一来源）

`proxy.py` 的 `/api/chat` **完全不掺和分流逻辑**（grep 零命中 `generate_stream/orchestrator/multi_intent/roles`）。它只做"鉴权 → 取消息 → `enqueue` 入内存队列 → 返回 SSE 订阅"（`proxy.py:597` 的 `chat()`，`:899` 的 `enqueue`）。

真正的分叉在消费队列的 Worker 内：

```
queue.py:836  intent = await detect_intent_v2(...)      # 意图分类
queue.py:862  decision = intent.get("decision")          # "route" / "split" / ...
queue.py:992  if decision == "split":                    # ← 分叉点 A：多意图
                orch = RoleOrchestrator() if ROLE_ORCHESTRATOR_ENABLED else Orchestrator()
                async for e in orch.execute(sub_tasks, ...): ...
queue.py:1162 else:                                      # ← 分叉点 B：单意图
                async for e in run_skill(skill_name, ...): ...
```

`decision == "split"` 的来源链路：
`cascade.py:619` 先 `route` → `recognize_intents` → 若 `is_multi` 则把 `decision` 改写为 `"split"`（`:627`）。

### 1.2 两套"规划逻辑"分别是什么

| | 逻辑 A（单技能内嵌） | 逻辑 B（roles 体系） |
|---|---|---|
| 位置 | `skills/agent_generate_site.py` 的 `generate_stream` 内部 | `roles/` 4 角色 + `RoleOrchestrator` |
| 规划 | `SYS_PLANNER` + `_chat` + `_parse_plan` + `await_confirm`（`:659–696`） | `ProductAgent` 包 `agent_requirement` 出 PRD |
| 开发 | `SYS_CODER` + `astream_with_fallback`（`:713–732`） | `DevAgent` 只 `inject_context` 注入"你以开发工程师身份"（`dev.py` + `orchestrator.py:56`） |
| 评审 | `_review` ≤3 轮（`:748–786`） | `QAAgent` 包 `agent_review` 出 ReviewReport |
| 本质 | **真执行**：自己规划、自己写、自己审 | **只包不干**：只追加身份提示 + 从文本解析交接物 |

### 1.3 roles 是"薄壳"的铁证（`file:line`）

四个 role 文件**没有任何 `run/execute` 业务方法**，只有三类声明式能力（`base.py:35/42/56`）：

```python
# roles/base.py:35  system_prompt_fragment()  → 只返回"你以XX身份工作"的中文
# roles/base.py:42  inject_context()          → 消息尾部追加一条 system 提示
# roles/base.py:56  capture_handoff()         → 从产出文本正则/JSON 解析交接物
```

```python
# roles/product.py:15  class ProductAgent(RoleAgent): role="product"; owned_skills=["agent_requirement"]
# roles/design.py:15   class DesignAgent(RoleAgent): role="design";  owned_skills=["agent_design"]
# roles/dev.py:16      class DevAgent(RoleAgent):    role="dev";     owned_skills=["agent_build","agent_generate_site","agent_doc"]
# roles/qa.py:16       class QAAgent(RoleAgent):     role="qa";      owned_skills=["agent_review"]
```

而 `RoleOrchestrator._enrich`（`roles/orchestrator.py:47`）对父类只做两件事：
```python
enriched = agent.inject_context(enriched, shared_ctx)   # 往消息尾部塞一句身份提示
...
result = await super()._run_one(...)                    # 真正干活还是父类 → run_skill
handoff = agent.capture_handoff(result.output_text, ...)# 从文本里揪交接物
```

**关键事实**：两条路径最终都汇聚到 `run_skill("agent_generate_site")` → **同一个 `generate_stream`**（`orchestrator.py:286` 的 `_run_one` 调 `run_skill`）���单意图裸跑；多意图多套一层 DevAgent 身份壳——但干活的还是同一份代码。

### 1.4 命名撞车，进一步放大"两套"观感

仓库里有**三个**叫 `orchestrator` 的文件：
- `app/orchestrator.py` —— 业务侧部署扩缩容（`run_scale/run_start/run_stop`），与意图编排**无关**；
- `app/agent/core/orchestrator.py` —— 真正的多意图 `Orchestrator`；
- `app/agent/roles/orchestrator.py` —— `RoleOrchestrator`（前者的子类）。

### 1.5 结论：为什么你会觉得"两套框架两套逻辑在跑"

- **单意图请求**：只有逻辑 A（skill 内部三步），没有角色壳；
- **多意图请求**：逻辑 A（skill 内部三步）+ 逻辑 B（角色壳）**叠在一起**跑同一个 skill → "规划被规划两次、评审被评审两次、开发被安了个身份壳"；
- 入口 `proxy.py` 看起来"没分支但其实有"，执行逻辑被一道队列隔开，肉眼难辨；
- 三个 `orchestrator` 命名撞车。

---

## 二、目标架构：统一为"一个 Orchestrator + 一等 Role 执行单元"

设计原则（一句话）：**所有请求（单/多意图）都走同一条 Orchestrator 路径；role 成为真正干活的单元，而非提示壳；强 Schema 交接物是唯一通信契约。**

### 2.1 目标分层

```
┌─────────────────────────────────────────────────────────────┐
│  入口层  proxy.py /api/chat                                  │
│  （只做：鉴权 → 取消息 → enqueue → 订阅 SSE，不做任何分流）   │
└───────────────────────────────┬─────────────────────────────┘
                                 ↓ 入队
┌─────────────────────────────────────────────────────────────┐
│  调度层  Orchestrator（唯一）                                 │
│  意图分类 → 分解成 1..N 个子任务（DAG）→ 按序派给 Role       │
│  ★ 单意图也包一层（不再有 queue.py:992 的分叉）              │
└───────────────────────────────┬─────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────┐
│  执行层  Role Pipeline（一等公民，每个 role 有真正的 execute） │
│                                                               │
│  ProductAgent.execute() ──PRD──→ DesignAgent.execute()       │
│        └──DesignSpec──→ DevAgent.execute()                   │
│              └──CodeArtifact(HTML)──→ QAAgent.execute()      │
│                    └──ReviewReport──→ 合并                    │
│                                                               │
│  交接物 = 强 Schema（PRD / DesignSpec / CodeArtifact /        │
│  ReviewReport），role 之间只通过交接物通信                    │
└───────────────────────────────┬─────────────────────────────┘
                                 ↓ 合并
                           SSE 事件流 → 前端渲染
```

### 2.2 关键变化（对照现状）

| 现状痛点 | 目标架构 |
|---|---|
| 分叉在 `queue.py:992`（split vs 单技能） | **消除分叉**：所有请求进唯一 Orchestrator |
| `agent_generate_site` 内部 Planner/Coder/Reviewer 三步自成体系 | 三步**拆散下沉**到对应 role 的 `execute()` |
| roles 只注入身份提示、不干活 | roles 有真正的 `execute()`，调用底层工具/skill 干活 |
| 三个 `orchestrator` 命名撞车 | 业务部署那个**重命名**（如 `deployment.py`），消除歧义 |
| 单/多意图行为不一致 | 单意图 = "1 个子任务的 pipeline"，与多意图同构 |

---

## 三、两种优化路径对比

### 方案 A：收敛冗余层（保留单技能引擎，移除 roles 重叠）

- **做法**：保留 `agent_generate_site` 单技能三步引擎作为唯一执行器；把 `roles/` 薄封装层**降级为纯元数据/路由表**（只用于统计 `record_role_dispatch`、skill→role 映射），**移除** `inject_context` 提示注入和 `capture_handoff` 文本解析这两处与 skill 内部三步重叠的工作；关闭或删除 `ROLE_ORCHESTRATOR_ENABLED` 开关。
- **优点**：改动小、风险低、立刻消除"两层规划"；G1–G8 的 bug 修复可照常落地。
- **缺点**：**不符合你最初的"4 角色串联 pipeline"期望**——角色只是标签，不真正分工。
- **适合**：你更看重"先稳定、别大动"时。

### 方案 B：让 roles 真正接管（统一 Pipeline）← 推荐

- **做法**：
  1. 给每个 role 加真正的 `execute(ctx) -> Handoff` 方法，`ProductAgent` 调需求生成、`DevAgent` 调 Coder、`QAAgent` 调 Reviewer；
  2. 把 `agent_generate_site.generate_stream` 里的 Planner/Coder/Reviewer 三步**抽离**成可复用的阶段函数，分别由 dev/qa role 的 `execute` 调用；
  3. `Orchestrator` 成为唯一调度层，**单意图也包一层**（拆成 1 个子任务或直接派 dev role），删除 `queue.py:992` 的 split 分叉；
  4. role 之间通过**强 Schema 交接物**（PRD/DesignSpec/CodeArtifact/ReviewReport）串联，不再从文本里正则解析；
  5. 重命名 `app/orchestrator.py` → `app/deployment.py`，消除三个同名文件。
- **优点**：**真正符合你"产品→开发→样式→测试 + orchestrator 调度"的期望**；单/多意图同构，架构一眼清晰；职责单一，每个 role 各管一摊。
- **缺点**：改动大、需要回归测试（尤其是 G1–G8 已落地的确认续跑逻辑要迁到新 pipeline）；roles 是运行时惰性导入，需重启 7101 验证。
- **适合**：你希望架构"从根上清晰"、愿意接受一轮较大的重构时。

### 我的推荐：方案 B（分阶段落地）

理由：你最初明确说过"分几个 agent：产品、开发、样式、测试，还有 orchestrator 调度"，方案 A 只是把乱的地方藏起来，没实现你的真实意图；方案 B 才让那 4 个 role 名副其实。但方案 B 不必一步到位——可先完成"消除分叉 + roles 真正 execute + 强 Schema 交接物"，把"单意图建站"作为第一个打通的 pipeline，再逐步把多意图/样式/测试 role 接全。

---

## 四、分阶段实施计划（方案 B）

> 以下为规划，**本次不动代码**。确认后我从 P0 开始。

- **P0 命名与分叉清理**
  - `app/orchestrator.py` → `app/deployment.py`（消除三个同名文件歧义）
  - `queue.py:992` 的 `if decision=="split"` 分叉**收敛**：统一调用 `Orchestrator.execute`；单意图内部拆成 1 个子任务，走同一 DAG 执行器。
- **P1 roles 成为一等执行单元**
  - 给 `ProductAgent/DesignAgent/DevAgent/QAAgent` 加 `execute(ctx)`，分别调用需求/设计/编码/评审的底层能力；
  - 抽离 `agent_generate_site` 内部的 Planner/Coder/Reviewer 为可复用阶段函数，由对应 role 的 `execute` 调用。
- **P2 强 Schema 交接物**
  - 定义 `PRD / DesignSpec / CodeArtifact / ReviewReport` 数据结构（已在 v2.2.0 有雏形于 `handoff.py`）；
  - role 之间只传结构化交接物，删除 `capture_handoff` 的文本正则解析。
- **P3 收敛冗余**
  - 移除 `ROLE_ORCHESTRATOR_ENABLED` 开关（或保留为"是否启用产品/设计前置子任务"的语义，而非"是否套壳"）；
  - `run_skill` 退化为 role 内部的工具调用，不再作为与 orchestrator 平级的另一条路径。
- **P4 验证**
  - reset 数据 → 单意图"建个网站"端到端（应走 Product→Design→Dev→QA 四 role）→ 多意图"个人网站+天气"端到端（建站子任务走四 role，天气子任务并行产出文字）→ 确认续跑走断点（G1–G8 逻辑迁移验证）。

### 对 G1–G8 的影响

G1–G8 是在"不重构"前提下修的确认死循环/假成功/可视化等问题。架构优化后：
- **G1（破死循环）**：根因 `queue.py:1038` orchestrator 漏传 checkpoint，将在 P0/P1 随分叉收敛自然消除；
- **G3（去重方案卡）**：确认事件自带的 `skill` 字段在方案 B 下更干净（role 明确）；
- **G5（假成功）**：P1 中 role 的 `execute` 返回值需带"是否真出产物"，从根上杜绝 `code=0` 记成功；
- **G7/G8（trail 可视化）**：前端无需改，后端事件语义在方案 B 下更规整，可视化更易对齐。
- 结论：**G1–G8 的修复目标在方案 B 里被"架构性地"解决了大半**，不需要逐条打补丁。

---

## 五、验收标准（方案 B 落地后）

1. 入口 `proxy.py` 不再有任何分流逻辑；所有请求经唯一 `Orchestrator`；
2. 单意图"帮我做个电商网站"：日志可见 `ProductAgent→DesignAgent→DevAgent→QAAgent` 四 role 依次 `execute`，`artifacts` 表有 HTML 产物；
3. 多意图"个人网站+卖货+深圳天气"：建站子任务走四 role pipeline；天气子任务并行产出文字；确认后**不再重弹 await_confirm**（断点续跑）；
4. 仓库内**仅剩一个** `orchestrator`（意图编排），业务部署改名 `deployment.py`；
5. role 之间通信全部为强 Schema 交接物，无文本正则解析。

---

## 六、下一步

本文仅给出方案，**未改动任何代码**。请确认方向：

- 选 **方案 A**（小改、先稳定），或 **方案 B**（重构、符合你最初期望，*推荐*）；
- 若选 B，是否同意从 **P0→P1→P2→P3→P4** 的顺序落地，落地后再 reset+多意图 e2e+写测试报告。

确认后我开始执行；过程中会持续更新本文档与今日记忆。
