# 个人网站 + 卖货 + 深圳天气 —— 流程走查与无产物根因分析报告

> 分析对象：用户消��「我想创建一个个人网站，上面还可以卖货的，另外今天深圳天气咋样？」
> 会话：conversation_id=3 / project_id=3 / user_id=1
> 证据来源：`backend/logs/app.log`、`backend/app/logs/intent_observations.jsonl`、MySQL `artifacts`/`messages` 表
> 分析时间：2026-07-27 22:45

---

## 一、现象复述

用户反馈：这条消息「2 次确认弹窗后，现在没有看到生成物」。

经日志 + 数据库交叉核验，**现象属实且根因明确**：
- 这句话在会话里被**处理了 3 轮**（3 个不同 trace），每一轮都卡在「方案层」，没有一次真正进入「开发 / Coder 出 HTML」阶段；
- 数据库 `artifacts WHERE project_id=3` **行数 = 0**，即全会话零产物；
- 三次 assistant 回复内容分别是：425 字假大空话术、4 字 `📋 {`、48 字 `📋 极简个人商品橱窗…` —— **全是方案残片，没有一行真实 HTML**。

---

## 二、实际走过的流程（3 轮处理时间线）

### 第 1 轮（22:30:49 – 22:31:57，trace `t19fa3fc4f6c3d4c9b53d32ad8`）
用户连续发的第二条消息，前端未指定断点，直接全量执行：

1. 意图级联：`chat/casual` conf 0.33（新奇度兜底）→ 但多意图层 `multi_intent` 二次拆分命中 `is_multi=true`，拆成 `sub_0 建站` + `sub_1 查深圳天气`；
2. 启用 `RoleOrchestrator`，**并行编排**两个子任务（strategy=parallel，confirmed=set()）；
3. `sub_1` 天气：`agent_search` 跑完，**产物空**（无天气 API，仅回一句"不在我的专业范围内"）；
4. `sub_0` 建站：`agent_generate_site` 跑 28s，**code 长度=0、预览 URL=0 个**；planner 自我合理化「受限于纯静态前端环境…以静态 UI 组件占位」，直接跳过代码生成；
5. 编排完成标记 `成功=2 失败=0 部分交付=False` —— **虚假成功**；
6. 前端**无确认弹窗**，直接 `done`，主气泡输出 425 字「两个需求我都已经帮你搞定啦…已生成完整建站方案」——**欺骗性文案，实际无产物**。

### 第 2 轮（22:36:43 – 22:37:28，trace `t19fa401b567622476ea20d178`）
前端**断点续跑**（`resume=True after=-`，即全量重跑），前端指定 `skill=agent_generate_site`：

1. 再次多意图拆分 `is_multi=true`；
2. **SSE #0 `plan_preview`** → 前端弹出**确认弹窗 #1（执行计划卡）**；
3. 路由 `agent_generate_site`，Planner 完成，**但输出的方案 JSON 是损坏的**（`plan_title="{"`、`plan_goal=""`，标题被截断成左花括号）；
4. **SSE #4 `await_confirm`** → 前端弹出**确认弹窗 #2（方案卡）**；
5. `done`，但 `output=4 字符 "📋 {"` —— 主气泡只有损坏的方案残片，**无产物**。

### 第 3 轮（22:37:48 起，trace `t19fa402b2758e7f4718139048`）
用户在前一轮 `await_confirm` 点了确认 → 前端发 confirm → 后端又 `resume=True` 从头重跑：多意图拆分 → `plan_preview`（再次弹窗）→ planner → `await_confirm`（再次弹窗）→ `done`，主气泡 48 字 `📋 极简个人商品橱窗…`（依然是方案，无代码）。

**「2 次确认弹窗」的来源**：第 2 轮贡献了 `plan_preview` + `await_confirm` 两次弹窗；第 3 轮确认后又重跑并再次弹出同样两次。用户点完确认，看到的是"又弹一次"，始终到不了出代码那一步。

---

## 三、流程图

（见对话内联 SVG 流程图 —— 展示 输入 → 意图识别 → 多意图拆分 → 第1次并行编排(假成功) → 续跑 plan_preview(弹窗1) → planner(损坏JSON) → await_confirm(弹窗2) → 确认后 resume 死循环回 plan_preview → artifacts=0）

核心死循环：**await_confirm 的方案卡确认后，后端 resume 不是从断点（develop）续进，而是又从 planner 全量重跑，再次弹出 await_confirm**——永远不进入 Coder 出 HTML。

---

## 四、根因分析

### A. 多意图并行编排模式下，建站子任务未真正出代码
第 1 轮 `RoleOrchestrator` 把 `sub_0` 派给 `agent_generate_site` 时，后者在「子任务上下文」下由 planner 判定「纯静态环境无法交易 / 无天气 API → 以静态占位」，于是**跳过 Coder 生成**（code 长度=0）。建站本职就是出静态 HTML，这个判定逻辑本不该跳过代码生成。

### B. 断点续跑（resume）每次全量重跑 planner，且消息累积导致 planner JSON 损坏
续跑请求是 `resume=True after=-`（无游标 = 全量重来），且历史消息不断累积（msgs=4 → 6），planner 的上下文被撑乱，输出**格式损坏的 JSON**（`plan_title="{"`）。损坏的 plan 又被原样推进 `await_confirm`。

### C. await_confirm 确认后没有断点续进 develop → 确认死循环（致命）
这是「2 次确认弹窗后没生成物」的直接机制：方案卡确认事件应携带 `checkpoint=stage=develop`，让后端跳过 planner **直接进入 Coder 出 HTML**；但实际 resume 每次都从 planner 重头来，于是「确认 → resume → 又弹 await_confirm」无限套娃，永远到不了出代码阶段。

### D. 编排「假成功」掩盖失败
第 1 轮标记 `成功=2 失败=0`，但 `code=0`、产物空。虚假成功让前端无从发现失败，也阻断了重试/兜底。

### E. 意图路由抖动
第一级 `detect_intent` 把这句话判成 `chat/casual` 0.33（新奇度兜底，top_score 0.33 < 0.45），靠多意图层二次拆分才识别建站语义。但拆分后 decision 仍是 `split → route` 到 `agent_generate_site`，没有「先稳态 plan 再确认」的统一入口，导致续跑时反复在 planner 层空转。

---

## 五、佐证（数据库硬证据）

```
artifacts WHERE project_id=3  →  rowcount: 0        ← 全会话零产物

messages (conversation_id=3):
  user    "你好"
  asst    71字 自我介绍
  user    "我想创建一个个人网站，上面还可以卖货的，另外今天深圳天气咋样？"
  asst    425字 假大空「已生成完整建站方案」（实际无产物）
  user    (同上)
  asst    4字   "📋 {"                              ← 损坏方案残片
  user    (同上)
  asst    48字  "📋 极简个人商品橱窗…"               ← 仍是方案，无代码
```

---

## 六、修复建议（按优先级）

1. **【高】打通 await_confirm → develop 断点续跑**：确认事件必须带 `stage=develop` checkpoint；后端 resume 时跳过 planner，直接进入 Coder 出 HTML，杜绝「确认后从头重跑」死循环。
2. **【高】多意图建站子任务必须真出代码**：`agent_generate_site` 在子任务上下文下也要走完整 planner→develop→coder 链路；planner 不得以「无 API / 静态环境」为由跳过 HTML 生成（建站本就产出静态页）。
3. **【中】planner JSON 解析健壮化**：损坏 JSON（`title="{"`）要能回退或触发重生成，绝不准把损坏 plan 推给 `await_confirm`。
4. **【中】消除「假成功」**：`code=0` / 产物空应标记为失败或「部分交付」，触发前端提示与重试，而非 `成功=2`。
5. **【低】意图层直路由**：建站 + 天气这类强建站语义，应直接 `route → build/site` 并打 `multi` 标记，避免先 `chat/casual` 新奇度兜底再拆的抖动。

---

## 七、结论

这条「个人网站 + 卖货 + 深圳天气」消息**确实被正确识别为多意图**（建站 + 天气），但执行链路存在三处连环缺陷：
1. 并行编排时建站子任务不出代码；
2. 续跑每次重跑 planner 且输出损坏 JSON；
3. **await_confirm 确认后不进 develop，形成确认死循环**。

三者叠加 = 用户看到 2 次确认弹窗、却永远拿不到生成物。DB `artifacts=0` 是铁证。修复核心是「确认即进 Coder」，并让多意图建站子任务真正产出 HTML。
