# SeedAI 全链路重构规划方案 · v2（历史依据，已被最终规范替代）

> **状态：历史依据。自 2026-08-01 起已被《SeedAI全链路重构最终实施规范.md》替代，不得作为新代码、Schema、配置、测试或生产变更的直接依据。**
>
> 蓝本：《Agent 全链路执行总图·完整详版》v2.0（十阶段 + Guardrails 五层 + 12 条红线 + 15 条最佳实践）
> 约束：用户已授权**彻底重构**（不向后兼容）；只本地 commit 不 push；涉及 DB 结构变化用 `scripts/reset_all.py` 重置（现有数据直接 reset）。
> 与第一版(v1)的根本区别：**v1 保留太多旧资产**——roles/skills/chroma/analytics/前端几乎不动。本版按你要求严格照总图，把 **Agent / Skill / 意图 / 数据库结构 / 流程** 五个维度全部重做，不再"留用不动"。

---

## 〇、总图十阶段 ↔ 现有代码 严格映射（逐阶段判定：重做 / 改造 / 保留）

| 阶段 | 总图定位 | 现有实现现状 | v2 动作 |
|---|---|---|---|
| P0 请求接入 | Input Guard + 限流 + 会话加载 + 审计初始化 | `proxy.py` 有 `_sanitize_input`(8000字截断+XSS)，缺 injection 检测/限流/审计初始化 | **重做**：独立 S0 阶段 + `transport/` 拆出 |
| P1 记忆召回 | Memory Gate 召回门（规则60%+轻量LLM40%）→ 向量召回 → 预填 SIR | `knowledge/chroma.build_rag_context` 无门控，永远全量召回 9 集合 | **重做**：`s1_memory_gate.py` 双门（召回门+存储门） |
| P2 意图理解 | LLM 只出 SIR_delta（JSON Schema 强约束） | `intent/sir_prompt._extract_sir_delta` 已部分落地 ✅ | **保留内核 + 改造**：对齐总图 delta schema（tool_call/memory_hints/pending） |
| P3 DST | 纯函数 4 操作 + 快照回滚 | `intent/dst.apply_delta` 已落地 ✅，但**缺 old_SIR 快照链** | **保留 + 补快照**：`sir:snap:{cid}` Redis LIST LTRIM 10 |
| P4 意图切分 | 单/多意图判定 + DAG TaskList + 主从识别 | `intent/multi_intent.py` 已落地，`SubTask`(core/models) 有 deps/risk | **保留 + 对齐**：补 intent_schema 共享槽声明、主从标记 |
| P5 规则校验 | 四层校验（必填/格式/业务/权限+风险分级） | `rulesmatcher`/`tools`/`safety` 分散，`RiskResult(level1/2/3)` | **重做**：`s5_validator.py` 集中四层 + TOOL_RISK 表 |
| P6 Plan-and-Execute | ReAct（简单）/ Plan（复杂 DAG）+ Executor | `roles/Orchestrator` DAG 执行 + `RoleAgent.execute`；**无 ReAct 路径** | **重做执行层**：`s6_executor` ReAct + Plan 双模式 |
| P7 存储判定 | Memory Gate 存储门（漏斗：规则/LLM/TTL） | `chroma.save_memory` 闲聊也存 ✅ 污染 | **重做**：`s7_memory_store.py` 双门 + weak_store TTL |
| P8 Output Guard | 格式/安全/脱敏/一致 + 流式 | `qc.py` 是质量打分非护栏；无脱敏 | **重做**：`s8_output_guard.py` 纯规则多层 |
| P9 归档 | 审计落盘 + Token 统计 + 资源释放 + 熔断 | `tracing.py`/`trace_repos.py`/`observation.py` 部分 | **重做**：`s9_archive.py` 按总图 JSON 格式 + 资源释放 |

> 关键事实：现有 `agent/models.py` 已有 12 张表（`users/projects/conversations/messages/artifacts/traces/trace_events/feedbacks/qc_scores/usage_logs/user_states/intent_slots`），但**缺 SIR 快照表、task 表、session 审计表**——这是 DB 维度重做的核心缺口。

---

## 一、Agent 维度重构（角色 + 执行器）

### 1.1 现有 Agent 资产盘点
- `agent/roles/`：`product.py` / `design.py` / `dev.py` / `qa.py` + `base.py`(`RoleAgent`) + `orchestrator.py` + `handoff.py`（4 角色 SOP 编排，已落地上下文隔离 + 强 Schema 交接物）。
- `agent/core/`：`runner.run_skill` / `queue.py`(1588行·上帝文件，含 worker_loop 全部分支) / `orchestrator`(SubTask DAG) / `router.py`(detect_intent_v2)。
- `agent/intent/cascade.py`(844行) 既做理解又做路由决策。

### 1.2 重构后 Agent 结构（严格照总图 P6）
```
agent/
├── pipeline/                 # 【新】Stage 编排（见第三节）
├── roles/                    # 【改造】保留 4 角色，但只做"执行单元"，不再内含流程控制
│   ├── base.py               # RoleAgent 瘦身：只留 execute() 委托 skill + 交接物捕获
│   ├── product.py/design.py/dev.py/qa.py
│   └── planner.py           # 【新】P6 Plan 模式：LLM 生成 DAG Plan（替代硬编码 SOP 顺序）
├── executor/                 # 【新】执行器（总图 P6 核心引擎）
│   ├── react.py             # ReAct 轻量循环（max_steps=5/重复Action检测/超时30s）
│   ├── plan_executor.py     # Plan 模式 Executor（依赖解析/并行调度/单步执行/三级容错）
│   └── tool_guard.py        # 单步执行时的 Tool Permission 检查（low直跑/mid登录态/high阻断）
└── (intent/ knowledge/ 见二、三节)
```

### 1.3 关键改造点
1. **ReAct 路径补建**（`executor/react.py`）：
   - 触发条件：P4 判定 TaskList 仅 1 个 SubTask 且 `selected_skill.simple=True`（如问答/查询）。
   - 循环：`Thought→Action→Observation`，`max_steps=5`、`_detect_repeat_action`（同一 `Action+Args` 连续 2 次 → 终止）、`asyncio.wait_for(30s)` 硬超时、`is_cancelled` 用户中断。
   - 产出：`ExecutionResult`（status/success_steps/failed_steps/last_tool/tool_summary），供 P7 存储判定。
2. **Plan 模式保留 DAG 编排**（`executor/plan_executor.py` 吸收原 `orchestrator.build_layers` + `_run_one`）：
   - 保留 `build_layers`（Kahn 拓扑 + 环依赖兜底）。
   - 吸收 `SharedContext`（dep_outputs / handoffs 强 Schema 交接物）——**这是你已落地的好设计，保留但不放在上帝文件里**。
   - 三级容错（指数退避重试 → 换参/换工具 → Re-planning 或降级交付）对齐总图 ③-d。
3. **Planner LLM 生成 Plan**（`roles/planner.py`）：复杂任务用 LLM 把 SIR 拆成 `PlanStep[]`（step/tool/args/deps），替代 4 角色硬编码 SOP 顺序；简单任务跳过 Planner 直接 ReAct。
4. **`role_orchestrator_enabled` 开关删除**——v2 不再双轨，Plan/ReAct 是唯一执行入口。
5. **`queue.py`(1588行) 删除**——其 `worker_loop` 全部面条控制流由 `pipeline` 取代；`cascade.py` 路由决策上移。

---

## 二、Skill 维度重构（Tool 体系 + 风险分级）

### 2.1 现有 Skill 资产
- 8 个技能：注册在 `SkillRegistry`（`agent/registry/skill_registry.py`）：
  `agent_chat` / `agent_search` / `agent_design` / `agent_requirement` / `agent_generate_site` / `agent_build` / `agent_doc` / `agent_review` / `agent_delete`。
- 工具：`ToolRegistry`（`tool_registry.py`）+ `@tool` 装饰器，`risk` 仅 `safe/dangerous` 两档（**非总图 low/mid/high/critical 四档**）。
- 工具清单（`agent/tools/`）：`fetch_url` / `web_search` / `html_validate` / `cos_upload` / `browser_screenshot` / `image_generate` / `rag_retrieve` / `file_io`。

### 2.2 重构后 Skill 结构（严格照总图 P5 ④-a TOOL_RISK）
```
agent/
├── skills/                   # 【改造】技能=可被编排器调用的"完整能力单元"
│   ├── base.py              # Skill 基类：run(ctx) → SkillOutput；声明 simple/risk_level/permissions
│   ├── catalog/             # 【新】每个技能一个声明文件（含 JSON Schema 入参 + risk + 必填权限）
│   │   ├── chat.yaml / search.yaml / design.yaml / requirement.yaml
│   │   ├── generate_site.yaml / build.yaml / doc.yaml / review.yaml / delete.yaml
│   └── (现有 9 个 handler 保留逻辑，仅改为继承 SkillBase + 声明元数据)
├── tools/                    # 【改造】底层工具，对齐四档风险
│   └── (现有 8 个 tool 保留，risk 字段从 safe/dangerous 扩为 low/mid/high/critical)
└── registry/
    ├── skill_registry.py    # 【改造】追加 risk_level / required_permissions / simple 字段
    └── tool_registry.py     # 【改造】TOOL_RISK 四档 + Approval Gate 钩子
```

### 2.3 关键改造点（严格照总图 §TOOL_RISK / Layer 2）
1. **风险四级落地**（`tool_registry.py`）：
   ```python
   TOOL_RISK = {
     "rag_retrieve": "low",        # 任何登录用户可调用
     "web_search": "low",
     "html_validate": "low",
     "image_generate": "low",
     "cos_upload": "mid",          # 需登录 + 关键槽齐全
     "fetch_url": "mid",
     "file_io": "mid",
     "browser_screenshot": "mid",
     "agent_delete": "high",       # 需用户确认(Approval Gate)
     "exec_shell": "critical",     # 禁止常规 Agent 调用
   }
   ```
2. **Approval Gate**（`executor/tool_guard.py`）：`high` 风险 → 暂停执行，emit `await_confirm` 事件（带 `sub_task_id` + 操作摘要），等前端回传 `confirmed_subtasks` 重发后放行；`critical` → 系统直接 `SUB_BLOCKED` 拒绝（红线 #1）。
3. **Skill 元数据声明**（`skill.yaml`）：每个技能声明 `risk_level / required_permissions / simple(bool) / input_schema(JSON Schema)`。`simple=True` 的技能（如 chat/search/design）才允许走 ReAct 快速路径。
4. **权限检查下沉到 S5**：S5 四层校验第四层读 `TOOL_RISK` + `user_role`（从 P0 加载的 `UserIdentity` 来）做权限判定，而非散落在 `orchestrator._run_one` 的硬编码 `RISK_HIGH/RISK_MEDIUM` 分支。

---

## 三、意图维度重构（Intent + SIR + DST + 切分）

### 3.1 现有意图资产
- `intent_catalog.json`：约 30 个意图（chat_*/build_*/manage_*），每个有 `level1/level2/skill/required_slots`；`catalog.py` 提供 `skill_for(level1,level2)` 单一映射来源 ✅。
- `intent/dst.py`：DST 纯函数 `apply_delta` ✅（4 操作 + 4 冲突规则 + `compute_missing/derive_decision`），`SLOT_SOURCE_RANK` / `CROSS_INTENT_SLOTS` / `LOW_CONF=0.6` 常量已对齐总图。
- `intent/store.py`：`load_sir/save_sir/reset_sir`（Redis 热 + MySQL `intent_slots` 冷双写）✅，但**无 old_SIR 快照链**。
- `intent/multi_intent.py`：多意图拆分 + `SubTask` DAG（deps/risk）✅。

### 3.2 重构后意图结构（严格照总图 P2/P3/P4）
```
agent/intent/
├── catalog.py               # 【保留】skill_for 单一来源；扩展 schema 读取
├── intent_catalog.json      # 【改造】每条意图补 intent_schema 字段：
│                            #   shared_slots / optional_slots / risk_level / slot_formats
├── schema.py               # 【新】IntentSchema 加载 + 跨意图共享槽查询(总图 P4 "跨域槽位继承")
├── sir_prompt.py           # 【改造】对齐总图 delta schema：tool_call / memory_hints / pending
├── dst.py                  # 【保留内核 + 补快照】apply_delta 不变；新增 snapshot(old_SIR)
├── store.py                # 【改造】load_sir 同时回放快照链；save_sir 前 LPUSH 快照
├── splitter.py            # 【新】从 multi_intent 抽出纯切分器(P4)，主从识别
└── (cascade/common 路由决策上移到 pipeline S2/S4)
```

### 3.3 关键改造点（严格照总图）
1. **IntentSchema 表化**（总图 P4 "维护一张 intent_schema 表"）：
   - 在 `intent_catalog.json` 每个意图增加：`shared_slots`（如 `city/date` 跨 build_hotel/build_flight 共享）、`optional_slots`、`risk_level`、`slot_formats`（日期 YYYY-MM-DD / 正整数 / 枚举）。
   - 新增 `schema.py`：`get_shared_slots(intent_a, intent_b)` 供 P3 意图切换清槽 + P4 跨域继承。
2. **SIR_delta 对齐总图**（P2 输出 schema）：LLM 只出 `{meta, slots, constraints_add, tool_call, pending, memory_hints}`，`sir_prompt._extract_sir_delta` 的 Pydantic 模型补齐 `tool_call` / `memory_hints` 字段；`tool_call` 由系统决定是否执行（红线 #4）。
3. **DST 快照链**（P3 铁律 #5 / 总图"每轮保留 old_SIR 快照"）：
   - `store.save_sir` 在写入前 `r.lpush(f"sir:snap:{cid}", json.dumps(old_sir))` + `r.ltrim(...,0,9)`（最多 10 轮）。
   - `dst.rollback(cid, steps=1)` 返回 `old_SIR` 支持"还是按刚才的"回滚。
   - 审计 diff：`old → new` 完整记录（总图 P9 审计要求）。
4. **意图切分独立**（`splitter.py`）：从 `multi_intent.py` 抽出纯切分器，对齐总图 P4 三结果（单意图/多意图并行/多意图串行）；主意图失败=整体失败，从意图失败=降级交付（吸收现有 `OrchestratorResult.partial_delivery`）。
5. **指代/省略检测**（P1 规则2）：在 S1 召回门加 `含"按我之前"/"那个"/"还是"/"继续" → 必召回`，复用 Chroma `find_relevant_messages` 已存在能力。

---

## 四、数据库结构重构（MySQL + Redis + Chroma）

### 4.1 现有表（shared/models.py，12 张）
`users/projects/conversations/messages/artifacts/traces/trace_events/feedbacks/qc_scores/usage_logs/user_states/intent_slots`
- SIR 现状：仅存 `intent_slots` 表（每行一个会话的 SIR 根 JSON）+ Redis `intent:slots:{cid}`。**无快照表、无 task 表、无 session 审计表**。

### 4.2 新增/改造表（严格照总图 P9 审计 + P4 task + P3 快照）
```sql
-- ① SIR 快照链(总图 P3 回滚；Redis 热 + MySQL 冷双写，替代在 Redis LIST 里存 raw JSON)
CREATE TABLE sir_snapshots (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  conversation_id INT NOT NULL,
  turn_id INT NOT NULL,
  snapshot JSON NOT NULL,          -- old_SIR 完整快照
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_conv (conversation_id, turn_id)
);

-- ② Task 表(总图 P4/P6：DAG TaskList 持久化，支持断点续跑)
CREATE TABLE tasks (
  id VARCHAR(32) PRIMARY KEY,       -- sub_0 / sub_1
  conversation_id INT NOT NULL,
  parent_task_id VARCHAR(32),       -- 主任务(从意图依附主意图)
  goal TEXT,
  selected_skill VARCHAR(64),
  risk_level VARCHAR(16),           -- low/mid/high/critical
  dependencies JSON,                -- [sub_id,...]
  status VARCHAR(16),               -- 复用 SUB_* 常量
  is_primary BOOLEAN DEFAULT FALSE, -- 主从识别
  result_summary TEXT,
  artifacts JSON,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_conv (conversation_id)
);

-- ③ Session 审计表(总图 P9：每轮每条 PHASE 的输入/输出/耗时/Token)
CREATE TABLE session_audits (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  session_id VARCHAR(64) NOT NULL,
  user_id INT,
  conversation_id INT,
  turn_id INT,
  phase VARCHAR(16),                -- phase0_input_guard / phase1_memory_recall / ...
  status VARCHAR(16),
  time_ms INT,
  tokens_in INT, tokens_out INT,
  detail JSON,                      -- 阶段特定(召回 items/校验 checks/工具 steps)
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_session (session_id, turn_id),
  INDEX idx_conv (conversation_id)
);

-- ④ 存储判定表(总图 P7：Memory Gate 存储漏斗留痕，替代 chroma 里无差别写)
CREATE TABLE memory_storage_log (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id INT, project_id INT,
  decision VARCHAR(16),             -- store / weak_store / no_store
  item_type VARCHAR(32),            -- user_profile / episodic / weak_pref
  content_hash CHAR(32),
  ttl_days INT DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user (user_id)
);

-- ⑤ intent_slots 改造：保留，但增加 turn_id 列(与快照链对应)，reset 只删当前会话
ALTER TABLE intent_slots ADD COLUMN turn_id INT NOT NULL DEFAULT 0;
```

### 4.3 Redis 键空间重构
| 旧键 | 新键 | 说明 |
|---|---|---|
| `intent:slots:{cid}` | 保留 | SIR 当前态（热） |
| （无） | `sir:snap:{cid}` | SIR 快照链 LIST LTRIM 10（原想在 Redis 存，改为 Redis 热+MySQL `sir_snapshots` 冷双写，符合你"热+冷"既有模式） |
| `ai:role:*` / `ai:orch/*` | `ai:stage:*` 系列 | 每阶段打点（见审计） |
| `cancel:{tid}` / `clients:{tid}` | 保留 | SSE 断连取消(C1)机制✅保留 |
| `ai:tool:idek:*` | 保留 | 工具幂等✅保留 |

### 4.4 Chroma 九集合对齐（总图 P1/P7 分类）
保留现有 9 集合（`components/memory/user_preferences/project_memory/project_code/error_patterns/intents/conversation_context/cache_gen`），但**召回与存储都过 Memory Gate 双门**：
- 召回：`build_rag_context` → 改为 `s1_memory_gate` 调用，先门控再按 `recall_user/recall_knowledge/recall_episodic` 分支检索（TopK + 相似度阈值 0.6 过滤）。
- 存储：`save_memory` → 改为 `s7_memory_store` 调用，漏斗判定 `store/weak_store/no_store`，weak_store 带 `ttl=30天` metadata（总图 P7 分支 B）。
- **去重更新**：存储前 `where user_id/type + content_hash` 查重，相似度 < 0.1 更新不新增（吸收 chroma.py 已有 `_short_hash` 逻辑）。

### 4.5 重置与迁移
- `scripts/reset_all.py` 增补 4 张新表 DROP+重建；**现有数据一律 reset**（用户未要求迁移历史会话）。
- 超管 `huzhen/huzhen189` 仍由 `reset_all.py` 创建（不在 `init_db` 建账号，铁律保留）。

---

## 五、流程维度重构（Pipeline + 10 Stage + TurnContext）

### 5.1 核心设计（严格照总图时序）
把 `queue.py`(1588行) 的面条控制流重构为**显式 10 阶段流水线**（v1 已提，v2 强制全部落到新结构，不再"旧链路并存"）：

```
main.py → POST /api/chat → enqueue(turn)
  ↓
pipeline.py (编排器, ~250行, 自研不用LangGraph)
  ├─ S0 gateway/        Input Guard(injection+XSS+超长+敏感词) + 限流 + 会话加载 + 审计初始化   [纯规则]
  ├─ S1 memory_gate/    召回门(规则60%+轻量LLM40%) → 向量召回 → 预填 SIR                          [规则+LLM]
  ├─ S2 understand/     LLM 出 SIR_delta(对齐总图 schema)                                          [LLM]
  ├─ S3 dst/            apply_delta(4操作) + 快照 old_SIR + 回滚锚点                                [纯函数]
  ├─ S4 splitter/       单/多意图判定 + TaskList(DAG,主从标记) + 跨域槽继承                         [规则+LLM]
  ├─ S5 validator/      四层校验(必填/格式/业务/权限+TOOL_RISK) → clarify/confirm/block 决策出口     [纯规则]
  ├─ S6 executor/       ReAct(简单,simple=True) / Plan(复杂DAG) + Tool Guard + Approval Gate        [LLM+规则]
  ├─ S7 memory_store/   SIR 回写 + 存储门(漏斗) → Chroma(去重+TTL)                                  [规则+LLM]
  ├─ S8 output_guard/   格式校验 + 脱敏(分级打码) + 一致性(订单/价格/URL 溯源)                       [纯规则]
  └─ S9 archive/        session_audits 落库 + Token 统计 + 资源释放 + 熔断检查                       [纯代码]
```

### 5.2 TurnContext（全链路唯一状态容器，唯一真相源）
```python
@dataclass
class TurnContext:
    # P0 填充
    user: UserIdentity            # user_id/role/vip (服务端鉴权, 不信任前端)
    session: SessionInfo          # session_id/conversation_id/project_id/turn_id
    raw_message: str
    clean_message: str
    trust: TrustFlags             # untrusted(注入标记)/truncated
    # P1 填充
    recall: RecallResult          # 召回的画像/知识/episodic + 判定原因 + 门控决策
    # P2/P3 填充
    sir_old: SIR | None           # 本轮前快照(回滚锚点)
    sir_delta: SIRDelta | None
    sir: SIR                      # DST 合并后的真理之源
    # P4/P5 填充
    tasks: list[SubTask]          # DAG TaskList(含主从/依赖)
    validation: ValidationResult  # action=route/clarify/confirm/block + 精准追问
    # P6 填充
    execution: ExecutionResult    # 各步骤 status/last_tool/tool_summary/失败原因
    # P8 填充
    reply_draft: str
    reply_final: str              # 脱敏后
    # 全程追加
    audit: list[PhaseAudit]       # 每阶段 {phase, status, ms, tokens, detail}
```

### 5.3 铁律落地为代码约束（v1 已提，v2 强化）
1. **规则在前**：纯规则阶段（S0/S3/S5/S8/S9）**禁止 import LLM 客户端**（import-linter 自定义规则，CI 门禁）。
2. **LLM 只出 Delta**：S2 的 LLM 只产 `SIR_delta`，DST(S3) 纯函数合并（红线 #2）。
3. **每轮快照**：S3 保存 `old_SIR`（红线 #5）。
4. **每阶段审计**：S9 把 `audit[]` 批量写 `session_audits` 表，格式对齐总图 P9 JSON。
5. **每阶段打点**：`ai:stage:{name}:{count|ms|fail}`（复用 analytics.py，遵守 `ai:` 前缀），SSE 可发 `stage` 事件给前端做进度条。

### 5.4 proxy.py 三拆（1879行 → 三文件）
- `transport/sse.py`：SSE 收发 + **C1 断连自动取消**（`cancel:{tid}` 置位 + `clients:{tid}` 计数）✅保留。
- `transport/session_api.py`：续传/对账/暂停恢复（从 proxy.py 拆出）。
- 鉴权/配额 → 并入 S0 gateway（P0 阶段职责）。

---

## 六、Guardrails 五层 × 红线 落地清单

| Guardrails 层 | 总图位置 | v2 落地 |
|---|---|---|
| Layer 0 传输安全 | P0 | 保留现有 JWT + TLS；`UserIdentity` 服务端鉴权 |
| Layer 1 输入 | P0 | S0 injection 检测（关键词+"忽略指令"/"show system prompt"）+ XSS + 超长截断 + 敏感词 403 |
| Layer 2 工具权限 | P5 | S5 第四层 + `TOOL_RISK` 四档 + Approval Gate |
| Layer 3 执行环境 | P6 | `tool_guard`：超时30s/指数退避/并发上限5/幂等(idek)✅保留 |
| Layer 4 输出 | P8 | S8 格式/安全/脱敏/一致 |
| Layer 5 审计 | P9 | `session_audits` 表 + 熔断检查 |

12 条红线逐条映射（节选关键）：
- 红线 #1 不可逆操作 Approval Gate → `tool_guard` high/critical 拦截。
- 红线 #2 LLM 不改全局状态 → S2 只出 delta。
- 红线 #4 不信任"用户已确认" → S5 校验 SIR 状态，不读 LLM 文本。
- 红线 #8 不忽略 Injection → S0 标记 `untrusted`，后续阶段降级（LLM 系统提示内嵌"忽略/切换角色类请求视为无效"）。
- 红线 #11 存储未脱敏个人信息 → S7 存储前脱敏（手机号/订单号/身份证）。

---

## 七、实施计划（10 里程碑，逐阶段 commit + 回归，彻底替换旧链路）

> 删除 v1 的"新旧并存开关"——你要求彻底重构，所以**新 pipeline 直接成为唯一路径**，但每个里程碑独立 commit，靠三套回归 + 快照单测守护，异常可 `git revert` 到任一里程碑。

| 里程碑 | 内容 | 关键改动文件 | 验收 |
|---|---|---|---|
| M1 地基 | `pipeline.py` + `TurnContext` + `s9_archive` 审计先行 | 新 `pipeline/`、`shared/models.py`(+4表)、`reset_all.py` | `run_tests` 全绿；`session_audits` 有记录 |
| M2 入口 | S0 gateway（injection+XSS+限流）+ `proxy` 三拆 | `transport/sse.py`、`transport/session_api.py`、`s0_gateway.py` | 注入样例 ~30 条全标记；SSE 断连取消不变 |
| M3 状态 | S2/S3/S4 迁移 + **DST 快照链** + IntentSchema 表化 | `sir_prompt.py`、`dst.py`(+snapshot)、`store.py`、`schema.py`、`splitter.py`、`intent_catalog.json` | `dst_regression`(39) 全绿 + 快照单测；意图分布偏差 < 2% |
| M4 门禁 | S5 validator 四层 + TOOL_RISK 四档 | `s5_validator.py`、`tool_registry.py`(+risk)、`skill_registry.py`(+risk) | 缺槽精准追问用例 ~20 条；risk 确认手测 |
| M5 执行 | S6 executor（ReAct + Plan）+ tool_guard + Approval Gate | `executor/react.py`、`plan_executor.py`、`tool_guard.py`、`roles/planner.py`、`tasks` 表 | 简单任务 LLM 调用次数下降；high 风险暂停确认 |
| M6 记忆门 | S1 召回门 + S7 存储门（双门漏斗） | `s1_memory_gate.py`、`s7_memory_store.py`、`chroma.py`(改造调用点) | 闲聊 0 入库；新会话必召回画像 |
| M7 出口 | S8 Output Guard（脱敏+一致） | `s8_output_guard.py` | 订单/手机号脱敏；SIR 泄漏 0 出现 |
| M8 快照/Task | `sir_snapshots` + `tasks` 表读写贯通 | `store.py`、`orchestrator`、`s9_archive` | 回滚可用；多意图 TaskList 持久化 |
| M9 删旧链路 | 删除 `queue.py` / `cascade.py` 路由部分 / `orchestrator` 旧 worker | 删文件 + 路由改挂 pipeline | ruff+mypy 门禁；单文件 < 800 行 |
| M10 文档 | 同步 `OPTIMIZE_PLAN.md` + `docs/` + 跑 `_e2e_20_abcde.py`(e2e20_seedai_test/testpass123) 出报告 | 文档 + 测试 | E2E 20 场景通过；测试账号段写入报告 |

### 验收总标准（v2 收紧）
1. **五维度全改落地**：Agent(ReAct+Plan)/Skill(四档风险+Approval)/意图(IntentSchema+快照)/DB(+4表)/流程(10 Stage) 全部就位。
2. **功能不回退**：`run_tests.py`(200) + `multi_intent_regression.py`(24) + `dst_regression.py`(39) + `_e2e_20_abcde.py`(20) 全绿。
3. **结构达标**：无 800 行业务文件；纯规则阶段零 LLM import（import-linter）。
4. **可审计**：任意请求 `session_audits` 表可见十阶段耗时/状态/token。
5. **新能力生效**：injection 标记、缺槽先追问、闲聊不入向量库、回复脱敏、SIR 可回滚、`high` 风险暂停确认。
6. **成本可见**：`ai:stage:*` 回答"哪个阶段最贵/最慢"。

---

## 八、与 v1 的差异总结（你要求更彻底的落点）

| 维度 | v1 动作 | v2 动作（本版） |
|---|---|---|
| Agent | roles/skills 保留 | 拆出 `executor/`（ReAct+Plan）重写执行层，删 `queue.py` 上帝文件 |
| Skill | 保留 9 技能 | 风险扩为四档 + Approval Gate + 每个技能元数据声明（simple/risk/permissions） |
| 意图 | 保留 catalog/dst | 补 IntentSchema 表化 + DST 快照链 + 切分器独立 |
| 数据库 | "基本不动" | **新增 4 张表**（snapshot/task/audit/storage_log），Redis 快照键 |
| 流程 | 新旧并存开关 | 直接单链路替换，10 里程碑逐步 git commit，靠回归守护可 revert |

---

## 九、待你拍板的 5 个问题（v2 细化）

1. **脱敏默认开还是关？** 建议 M7 先只开格式+一致（内部测试），脱敏打码上生产前再开（涉及展示效果，需你确认文案）。
2. **轻量 LLM 兜底用哪个档位？** Memory Gate 模糊判定建议 `qwen-turbo`（单次 < 100 token），是否同意？还是用现有 `deepseek` 小模型？
3. **M3 前是否先跑 200 条基线？** 建议开工前 `run_tests.py --quick` 存 `ai:` 埋点快照，作 M3/M6 对比基线。
4. **`queue.py` 删除时机**：v2 删旧链路放在 M9（而非 v1 的"开关并存"），你接受"无回退开关、靠 git revert"的激进策略吗？还是仍要保留 `PIPELINE_V3_ENABLED` 过渡？
5. **现有 8 技能是否够？** 总图示例有 `send_email/delete_file/exec_shell` 等，当前产品是建站助手，是否需要在重构时**补 `critical` 级工具占位**（如未来 deploy 能力），还是只做现有 9 技能的四档标定？

---

> 文档版本 v2.0 · 2026-07-30（夜）
> 蓝本：《Agent 全链路执行总图·完整详版》v2.0
> 摸底依据：backend/app 全量代码直读（catalog/dst/store/orchestrator/chroma/models/tool_registry/skill_registry/intent_catalog 等），非子代理二手报告
