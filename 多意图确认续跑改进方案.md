# 多意图「确认后续跑」改进方案

> 目标：让用户的多意图消息（如"个人网站+卖货+深圳天气"）**按 需求→方案→开发→产物 的既定流程走到出代码**，确认是一次性门、确认后不再重复弹框、且多意图不塌缩。
> 本文先给方案，**确认后再进开发环境落地**（G1–G8）。

---

## 一、症状回顾（已用 DB 铁证确认）

用户消息：`我想创建一个个人网站，上面还可以卖货的，另外今天深圳天气咋样？`

- 现象：弹了 2 次确认弹窗后，**没有看到生成物（建站 HTML 缺失）**。
- DB 铁证：`artifacts WHERE project_id=3` **= 0 行**；三次 assistant 回复全是方案残片（425 字空话 / 4 字 `📋 {` / 48 字 `📋 极简个人商品橱窗…`），**无一行真实 HTML**。

---

## 二、当前链路真相（已代码勘察，附 file:line）

| 环节 | 位置 | 现状 |
|------|------|------|
| 发 `await_confirm` | `agent_generate_site.py:696` | `paused(stage=await_confirm)`，**无 `skill` 字段**；发完 `return`（Coder 在 return 之后，死代码） |
| 前端硬编码 skill | `ChatView.vue:1068` | `skill: 'agent_generate_site'`（因后端事件没带） |
| 确认回传 | `ChatView.vue:300` + `chat.ts:86` | `GET /api/chat?resume=true&skill=agent_generate_site` |
| resume 锁 skill | `proxy.py:783` | `payload["skill"] = "agent_generate_site"`（整体塌缩单技能） |
| 强制单技能路由 | `queue.py:874` | `if (skill or checkpoint): decision = "route"`（绕开 orchestrator） |
| checkpoint 透传 | `queue.py:1172` | 单技能路径才透传给 `generate_stream` |
| checkpoint 跳 Coder | `agent_generate_site.py:550` | `await_confirm` → `planner_done` → 重跑 Coder |
| **orchestrator 漏传** | **`queue.py:1038`** | **`orch.execute()` 没传 `checkpoint/resume_mode`** |
| Coder 重复生成 | `agent_generate_site.py:583` | checkpoint 分支每次从零 `astream_with_fallback`，不复用已生成 HTML |

### 关键结论
1. **当前代码已有一道"缓解"**：resume 时 `queue.py:874` 强制单技能 + `proxy.py:783` 锁死 skill + `queue.py:1172` 透传 checkpoint，使建站子任务 resume 后能跳到 Coder 出 HTML——所以"建站子任务本身"在 resume 后理论上能出产物。
2. **但根因类缺陷仍在**，且精确解释你看到的症状：
   - **塌缩丢意图**：resume 把多意图塌缩成单技能 `agent_generate_site`，**天气 sub_1 被整体丢弃**（partial delivery）。
   - **死循环隐患**：`queue.py:1038` 没把 checkpoint 透传给 orchestrator。一旦 resume 因任何原因重回 `split` 分支（如 Redis 不可用、`ck_get` 返回 None、或未来移除 `queue.py:874` 的强制 route），建站子任务的 `generate_stream` 就拿不到 checkpoint → 重跑 Planner → 再发 `await_confirm` → **无限确认循环**。
   - **假成功**：编排 `merge` 阶段 `code=0` 仍记 `成功=2`，掩盖失败。
   - **plan_preview 重复弹**：每次 resume 都会再发一次方案卡。

---

## 三、设计原则

**方案优先、确认即门、断点续跑、流程闭环**

1. **确认是一次性门**：确认后直接进入「开发 / Coder」，不再重复弹方案卡 / 确认框。
2. **写代码步骤严格按流程走**：需求 → 方案 → 开发 → 产物，必须走到出 HTML。
3. **多意图不塌缩**：按子任务续跑，天气子任务（low risk）正常完成，不被丢弃。
4. **过程可见、结果收敛**：子任务提前完成先实时推到 trail-wrap，最终合成回复统一覆盖。

---

## 四、改进项（G1–G8）

### G1 断点续跑透传 orchestrator（破死循环根因）
`queue.py:1038` 的 `orch.execute(...)` 增加：
```python
checkpoint=job.get("checkpoint"),
resume_mode=job.get("resume_mode"),
```
`orchestrator._run_one` 已用 `**extra_kwargs` 透传 → 即使 resume 走 `split` 分支，建站子任务也能拿到 checkpoint 跳到 Coder，不再重跑 Planner、不再发 `await_confirm`。

### G2 多意图按子任务续跑（不再塌缩）
- `agent_generate_site.py:696` 的 `paused` 事件加 `sub_task_id` 字段。
- 前端确认回传 `sub_task_id`（`ChatView.vue:300` 处）。
- 后端 resume 仅对该子任务注入 checkpoint；天气 sub_1（low risk）正常跑完。
- 取消 `proxy.py:783` 的"整体锁死单技能"，改为"按 `sub_task_id` 路由对应子任务"。

### G3 await_confirm 自带 skill + plan_preview 去重
- `agent_generate_site.py:696` 加 `skill="agent_generate_site"`，前端去掉硬编码（`ChatView.vue:1068`）。
- resume 时**不再重发 plan_preview**：checkpoint 分支跳过 plan_preview 发送（判断 `resume_mode` 即可），方案卡只在首次 Planner 产出时弹一次。

### G4 checkpoint 复用已生成 HTML（不重复 Coder）
- `agent_generate_site.py:583`：若 DB/Redis 已有该对话的 preview url，直接 `yield ev("preview", url=...)` 复用，跳过 `astream_with_fallback`。
- 首次 Coder 产物已存 `artifacts` 表，可查。

### G5 假成功修复
- orchestrator `merge`：子任务 `code_length==0` 或 `preview_url` 为空 → 标记 `fail`，不计入 success。
- `partial_delivery=True` 时前端明确提示"部分完成：建站已生成 / 天气获取失败"。

### G6 Planner 腐蚀 JSON 健壮回退
- resume 时 Planner 输入用**首跑快照**（首跑 user_msgs 存 Redis），不累积全量消息历史，避免 JSON 损坏。
- `_extract_plan` 增加容错（花括号配对、截断修复），解析失败回退默认方案。

### G7 流程可视化（trail-wrap 阶段进度）
- 当前会话的 `trail-wrap-inline` 内显示「方案已确认 → 开发中 → 产物已生成」三阶段进度条，呼应之前的前端改动，让用户**直观看到"写代码步骤按流程来"**。

### G8 子任务提前完成即推结果到 trail-wrap（实时展示，最终合并覆盖）
- 后端多意图 `orchestration` 已发 `subtask_done` 事件（`orchestrator.py:318`），携带该子任务的结果（文字/url）。
- 前端在 `trail-wrap-inline` 内新增"已完成子任务"区：**监听到 `onSubtaskDone` 时，立刻把该子任务的结果摘要（如"✅ 深圳天气：晴 28°C"）push 进 trail-wrap 展示**，不等全部子任务结束。
- 最终 `merge` 事件触发后，主气泡的合成回复会整合这些碎片；`trail-wrap` 在 `mergeResult` 到达后按既有机制切换为合并结果卡（MergedResult），自然"覆盖掉"提前推送的零散结果。
- 效果：**执行中能看到"谁先跑完、结果是什么"**；合并后统一成最终回复，符合你说的"反正最终合成的回复会覆盖掉它"。

---

## 五、验收标准

- 发"个人网站+卖货+深圳天气" → **弹 1 次方案卡** → 确认 → 断点续跑直接进 Coder → 出建站 HTML 产物；天气子任务另出文字并即时显示在 trail-wrap。
- 确认后**不再重复弹**方案卡 / 确认框。
- `artifacts` 表应有 ≥1 行（建站预览）。
- 多意图不塌缩，天气意图有回应；子任务提前完成先显示在 trail-wrap，合并后被最终回复覆盖。
- `vue-tsc --noEmit` 对 `ChatView.vue` 无新错误；后端 `py_compile` 通过。

---

## 六、实施顺序建议

```
G1 断点透传（破死循环根因）
 → G3 去重 + skill 自带
   → G6 Planner JSON 健壮
     → G5 假成功修复
       → G2 多意图按子任务续跑
         → G4 复用已生成 HTML
           → G7 流程可视化
             → G8 子任务提前完成推 trail-wrap
```

> 注：G1 是致命根因，优先做；G2 改动面最大（牵动 orchestrator + 前端回传），放中段；G7/G8 纯前端展示，最后收尾。

---

## 七、真实全链路流程图与说明（用户输入 → 返回）

> ⚠️ **先说结论**：你怀疑"现在的流程不是 4 个角色串联"，**基本成立**。代码里 `roles/product|design|dev|qa.py` 这 4 个 RoleAgent **确实存在且被实例化**，但它们是**薄封装**——只往消息追加一句身份 system 提示、并从产出文本里解析交接物，**没有任何 `run/execute` 业务方法**。真正的 HTML / 文字产出全部由 8 个 **skill** 完成。
>
> **默认一条"帮我做个网站"的请求，根本不进 4 角色链**：意图 `build/site` → 单个 skill `agent_generate_site` → 该 skill 内部 Planner→(await_confirm 暂停)→Coder→Reviewer(内置自评)→Deliver。**`product/design/qa` 三个 RoleAgent 在这一路径零调用**；只有 `dev` 因映射被注入一句身份提示，无 SOP 串联。4 角色链**仅当用户显式发起"先出需求→再设计→再建站→再评审"这类带串行连词的复合多意图时才偶发**（DAG 执行 + 角色上下文增强，仍不是自治 Agent 串联）。

### 7.1 流程图（每一步标注 file:line）

```svg
<svg viewBox="0 0 720 1360" xmlns="http://www.w3.org/2000/svg" font-family="-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif">
  <defs>
    <marker id="arw" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto">
      <path d="M0,0 L8,3 L0,6 Z" fill="#64748b"/>
    </marker>
  </defs>
  <rect x="0" y="0" width="720" height="1360" fill="#ffffff"/>
  <text x="360" y="32" font-size="18" font-weight="700" fill="#0f172a" text-anchor="middle">真实全链路：用户输入 → 返回（附 file:line）</text>
  <text x="360" y="52" font-size="11" fill="#64748b" text-anchor="middle">单进程 FastAPI · 默认建站走单 skill（非 4 角色串联）· 多意图才偶发 orchestrator</text>

  <!-- S1 -->
  <rect x="90" y="70" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="92" r="13" fill="#2563eb"/><text x="112" y="97" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">1</text>
  <text x="135" y="88" font-size="13" fill="#1f2933">前端：用户点发送 → send()/doSend() → openEs()</text>
  <text x="135" y="105" font-size="10.5" fill="#64748b">ChatView.vue:1182 / :1301</text>

  <!-- S2 -->
  <rect x="90" y="138" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="160" r="13" fill="#2563eb"/><text x="112" y="165" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">2</text>
  <text x="135" y="156" font-size="13" fill="#1f2933">GET /api/chat?q=文本&amp;convId=..（消息历史后端从 DB 重建，无 messages 字段）</text>
  <text x="135" y="173" font-size="10.5" fill="#64748b">ChatView.vue:1301；proxy.py:647</text>

  <!-- S3 -->
  <rect x="90" y="206" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="228" r="13" fill="#2563eb"/><text x="112" y="233" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">3</text>
  <text x="135" y="224" font-size="13" fill="#1f2933">后端入口 proxy.chat()：鉴权 → 配额 → _build_messages_from_db</text>
  <text x="135" y="241" font-size="10.5" fill="#64748b">proxy.py:597 / :647</text>

  <!-- S4 -->
  <rect x="90" y="274" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="296" r="13" fill="#2563eb"/><text x="112" y="301" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">4</text>
  <text x="135" y="292" font-size="13" fill="#1f2933">enqueue 同进程队列（resume/new 分支）→ Worker 由 lifespan 启动</text>
  <text x="135" y="309" font-size="10.5" fill="#64748b">proxy.py:892/899；queue.py:691；main.py:75</text>

  <!-- S5 -->
  <rect x="90" y="342" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="364" r="13" fill="#2563eb"/><text x="112" y="369" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">5</text>
  <text x="135" y="360" font-size="13" fill="#1f2933">Worker：detect_intent_v2 → classify_v3（混合级联 + 安全门控）</text>
  <text x="135" y="377" font-size="10.5" fill="#64748b">router.py:39；cascade.py:593</text>

  <!-- S6 -->
  <rect x="90" y="410" width="540" height="44" rx="10" fill="#eef2ff" stroke="#c7d2fe"/>
  <circle cx="112" cy="432" r="13" fill="#4f46e5"/><text x="112" y="437" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">6</text>
  <text x="135" y="428" font-size="13" fill="#1f2933">decision 分支 switch：route / split / clarify / block / confirm</text>
  <text x="135" y="445" font-size="10.5" fill="#64748b">queue.py:895</text>

  <!-- S7 -->
  <rect x="90" y="478" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="500" r="13" fill="#2563eb"/><text x="112" y="505" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">7</text>
  <text x="135" y="496" font-size="13" fill="#1f2933">默认 route → run_skill(agent_generate_site)【注意：不进 orchestrator】</text>
  <text x="135" y="513" font-size="10.5" fill="#64748b">queue.py:992/874；runner.py:80；只有 split 才进 RoleOrchestrator</text>

  <!-- S8 -->
  <rect x="90" y="546" width="540" height="44" rx="10" fill="#fff7ed" stroke="#fb923c"/>
  <circle cx="112" cy="568" r="13" fill="#ea580c"/><text x="112" y="573" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">8</text>
  <text x="135" y="564" font-size="13" fill="#1f2933">单 skill 内 Planner(LLM) 出方案 → paused(stage=await_confirm) 暂停</text>
  <text x="135" y="581" font-size="10.5" fill="#64748b">agent_generate_site.py:659 / :696（await_confirm 是 paused 的 stage 值）</text>

  <!-- S9 -->
  <rect x="90" y="614" width="540" height="44" rx="10" fill="#fff7ed" stroke="#fb923c"/>
  <circle cx="112" cy="636" r="13" fill="#ea580c"/><text x="112" y="641" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">9</text>
  <text x="135" y="632" font-size="13" fill="#1f2933">前端弹方案卡 → 用户确认 → resume GET ?resume=true&amp;skill=..</text>
  <text x="135" y="649" font-size="10.5" fill="#64748b">ChatView.vue:1062 (onPaused) / :276 (resendConfirmed)</text>

  <!-- S10 -->
  <rect x="90" y="682" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="704" r="13" fill="#2563eb"/><text x="112" y="709" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">10</text>
  <text x="135" y="700" font-size="13" fill="#1f2933">checkpoint 规整 planner_done → 跳 Coder（不重跑 Planner）</text>
  <text x="135" y="717" font-size="10.5" fill="#64748b">agent_generate_site.py:550 / :574</text>

  <!-- S11 -->
  <rect x="90" y="750" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="772" r="13" fill="#2563eb"/><text x="112" y="777" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">11</text>
  <text x="135" y="768" font-size="13" fill="#1f2933">Coder 流式 token + Reviewer 内置自评 ≤3 轮（非独立 qa Agent）</text>
  <text x="135" y="785" font-size="10.5" fill="#64748b">agent_generate_site.py:722 / :590</text>

  <!-- S12 -->
  <rect x="90" y="818" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="840" r="13" fill="#2563eb"/><text x="112" y="845" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">12</text>
  <text x="135" y="836" font-size="13" fill="#1f2933">_deliver：写本地 artifacts/ + 上传 COS → preview(url)</text>
  <text x="135" y="853" font-size="10.5" fill="#64748b">agent_generate_site.py:312 / :336</text>

  <!-- S13 -->
  <rect x="90" y="886" width="540" height="44" rx="10" fill="#f5f7fa" stroke="#c8d0da"/>
  <circle cx="112" cy="908" r="13" fill="#2563eb"/><text x="112" y="913" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">13</text>
  <text x="135" y="904" font-size="13" fill="#1f2933">proxy 落库：幂等 upsert Artifacts 表 + assistant 消息(JSON)</text>
  <text x="135" y="921" font-size="10.5" fill="#64748b">proxy.py:1452 / :1460</text>

  <!-- S14 -->
  <rect x="90" y="954" width="540" height="44" rx="10" fill="#ecfdf5" stroke="#86efac"/>
  <circle cx="112" cy="976" r="13" fill="#059669"/><text x="112" y="981" font-size="12" fill="#fff" text-anchor="middle" font-weight="700">14</text>
  <text x="135" y="970" font-size="13" fill="#1f2933">SSE 事件回前端 → 渲染：token 累积气泡 / 预览 iframe / refined 覆盖 / trail-wrap</text>
  <text x="135" y="987" font-size="10.5" fill="#64748b">proxy.py:910；ChatView.vue:908 (onToken) / :935 (onPreview)</text>

  <!-- 箭头连接 -->
  <path d="M360,114 L360,138" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,182 L360,206" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,250 L360,274" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,318 L360,342" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,386 L360,410" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,454 L360,478" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,522 L360,546" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,590 L360,614" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,658 L360,682" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,726 L360,750" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,794 L360,818" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,862 L360,886" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>
  <path d="M360,930 L360,954" stroke="#64748b" stroke-width="1.5" marker-end="url(#arw)"/>

  <!-- 4 角色真相面板 -->
  <rect x="40" y="1020" width="640" height="300" rx="14" fill="#f0fdf4" stroke="#86efac"/>
  <text x="60" y="1048" font-size="14.5" font-weight="700" fill="#166534">⚠️ 4 角色（产品 / 开发 / 样式 / 测试）真相</text>
  <text x="60" y="1074" font-size="11.5" fill="#374151">① roles/product|design|dev|qa.py 确实存在且被实例化，但只是<b>薄封装</b>：只追加一句身份 system 提示 + 解析交接物，无 run/execute 业务方法。</text>
  <text x="60" y="1098" font-size="11.5" fill="#374151">② 默认单建站请求：run_skill(agent_generate_site) 直接出 HTML，product/design/qa <b>零调用</b>；仅 dev 因映射被注入身份提示，无 SOP 串联。</text>
  <text x="60" y="1122" font-size="11.5" fill="#374151">③ 多意图 split（建站+天气）：拆成 sub_0=agent_generate_site(dev) + sub_1=agent_search(无角色)，<b>独立并行，无 4 角色链</b>。</text>
  <text x="60" y="1146" font-size="11.5" fill="#374151">④ 4 角色串联仅偶发：用户显式"先出需求→再设计→再建站→再评审"带串行连词时，RoleOrchestrator 才按 DAG 执行并注入角色上下文。</text>
  <text x="60" y="1170" font-size="11.5" fill="#374151">⑤ 结论：你担心的"流程不是这样"基本成立 —— 代码是 <b>单 skill 内部 3 步(Planner/Coder/Reviewer) + 仅复合多意图偶发的角色增强</b>。</text>
  <text x="60" y="1198" font-size="11" fill="#64748b">SSE 事件清单：intent / node(enter_planner→enter_coder→enter_reviewer→previewing→done) / plan / think /</text>
  <text x="60" y="1216" font-size="11" fill="#64748b">plan_preview / paused(await_confirm) / clarify / block / confirm / orchestration / subtask_start|done|fail /</text>
  <text x="60" y="1234" font-size="11" fill="#64748b">merge / token / preview / refined / review / qc / done / cancel_summary（发出位置见第六章 + events.py:3-24）</text>
  <text x="60" y="1262" font-size="11" fill="#9ca3af">注：paused 事件 stage=await_confirm 是"暂停等确认"，并非独立事件名；确认后 resume 跳 Coder，不重跑 Planner。</text>
  <text x="60" y="1290" font-size="11" fill="#9ca3af">改进 G1–G8 即围绕这条真实链路修补：破确认死循环(G1)、多意图不塌缩(G2)、子任务结果实时入 trail-wrap(G8)。</text>
</svg>
```

### 7.2 逐步说明（用户输入 → 返回）

| 步 | 发生了什么 | 关键代码 |
|----|-----------|---------|
| 1 | 用户在聊天框输入并点发送，`send()` 收集本轮文本，经 `doSend()` 调 `openEs()` | `ChatView.vue:1182 / :1301` |
| 2 | 前端发 **GET** `/api/chat?q=文本&convId=..`；**不带 messages / project_id**（消息由后端从 DB 重建，project_id 由 convId 反查） | `ChatView.vue:1301`；`proxy.py:733-735` |
| 3 | 后端 `proxy.chat()` 做鉴权、配额，并从 DB 重建完整消息历史 `_build_messages_from_db` | `proxy.py:597 / :647` |
| 4 | 任务 enqueue 进**同进程队列**；真正的处理在 Worker（`worker_loop`，由 `main.py` lifespan 启动） | `proxy.py:892/899`；`queue.py:691` |
| 5 | Worker 调 `detect_intent_v2` → `classify_v3` 做意图识别（混合级联 + 安全门控） | `router.py:39`；`cascade.py:593` |
| 6 | 按 `decision` 进入 switch：`route` / `split` / `clarify` / `block` / `confirm` | `queue.py:895` |
| 7 | 默认 `route` → `run_skill("agent_generate_site")`。**关键点：只有 `decision=="split"` 才进 `RoleOrchestrator`**，普通建站根本不碰 4 角色 | `queue.py:992/874`；`runner.py:80` |
| 8 | 单 skill 内部：Planner(LLM) 生成方案 → 发 `paused(stage=await_confirm)` 暂停，等前端确认 | `agent_generate_site.py:659 / :696` |
| 9 | 前端 `onPaused` 弹方案卡；用户点"确认并生成" → `resendConfirmed` 回发 `GET ?resume=true&skill=agent_generate_site` | `ChatView.vue:1062 / :276` |
| 10 | 后端 resume：checkpoint 规整为 `planner_done` → **直接跳 Coder，不重跑 Planner** | `agent_generate_site.py:550 / :574` |
| 11 | Coder 流式产出 token；Reviewer 做内置自评（≤3 轮 Reflexion）——**注意这是 skill 内自评，不是独立 qa Agent** | `agent_generate_site.py:722 / :590` |
| 12 | `_deliver`：写本地 `artifacts/` + 上传 COS，发 `preview(url)` 直链 | `agent_generate_site.py:312 / :336` |
| 13 | 对话结束 `proxy._persist_conversation`：幂等 upsert `Artifacts` 表 + 写 assistant 消息(JSON 含 artifact_id) | `proxy.py:1452 / :1460` |
| 14 | SSE 事件经 `proxy.subscribe` 回前端；`onToken` 累积气泡、`onPreview` 开 iframe、`onRefined` 覆盖终版文字、`trail-wrap` 渲染分步轨迹 | `proxy.py:910`；`ChatView.vue:908 / :935` |

### 7.3 多意图（建站 + 天气）在哪分叉

- 当 `classify_v3` 判定 `decision=="split"`（如"建站**并且**查天气"命中并行连词），才进入 `RoleOrchestrator`（`queue.py:992`）。
- `multi_intent.recognize_intents` 把一句话拆成 `sub_0=agent_generate_site(dev)` + `sub_1=agent_search(无角色)`，**两子任务独立并行，无 product→design→dev→qa 串联**（依赖边仅在出现"先…再/然后"等串行连词时才生成）。
- 子任务各自走自己的 skill；`orchestration / subtask_start|done|fail / merge` 事件用于前端进度。改进 **G2** 让 resume 按 `sub_task_id` 续跑、不塌缩；**G8** 让提前完成的子任务结果即时进 trail-wrap，最终 `merge` 合成覆盖。

---

## 八、附：改进项与流程图的对应关系

| 改进项 | 针对流程图的哪一步 | 解决什么 |
|--------|------------------|---------|
| G1 | 步 7/10（orchestrator 透传 checkpoint） | 破确认死循环根因 |
| G2 | 步 6/7（split 分支 + sub_task_id 续跑） | 多意图不塌缩，天气子任务不丢 |
| G3 | 步 8/9（skill 自带 + plan_preview 去重） | 确认后不再重弹方案卡 |
| G4 | 步 10/11（复用已生成 HTML） | 不重复跑 Coder |
| G5 | 步 11/13（code=0 标 fail） | 假成功修复 |
| G6 | 步 8（Planner 快照 + JSON 容错） | 抗病化 JSON |
| G7 | 步 14（trail-wrap 阶段进度条） | 流程可视化 |
| G8 | 步 14（子任务提前完成推 trail-wrap） | 实时展示、最终合并覆盖 |
