# SeedAI 全链路重构规划方案

> 目标：以《Agent 全链路执行总图·完整详版》(v2.0) 的十阶段模型为蓝本，把现有单进程后端重构为**阶段清晰、职责单一、可审计、可单测**的流水线架构。
> 依据：2026-07-30 代码摸底报告（backend/app 全量探索）。
> 约束：不需要向后兼容（用户已授权自由重构）；只本地 commit 不 push；涉及 DB 结构变化用 `scripts/reset_all.py` 重置。

---

## 一、现状诊断：有什么、缺什么

### 1.1 现状 ↔ 十阶段对照表

| 总图阶段 | 现状 | 现有文件 | 结论 |
|---|---|---|---|
| P0 请求接入/输入护栏 | 部分：XSS 过滤、8000 字截断、配额限流 | `proxy.py:_sanitize_input`、`metrics.py` | ⚠️ 缺 Prompt Injection/越狱检测 |
| P1 记忆召回 | 有召回但**无 Gate 判定**，靠相似度阈值隐式触发 | `knowledge/chroma.py` | ⚠️ 缺 Memory Gate（规则粗筛+轻量LLM兜底） |
| P2 意图理解/SIR_delta | 强：混合级联(规则→向量→LLM)、SIR delta | `intent/cascade.py`(844行) | ✅ 保留，但需拆解耦 |
| P3 DST 状态更新 | 强：`apply_delta` 纯函数、4操作、置信度门控 | `intent/dst.py` | ⚠️ 缺 old_SIR 快照/回滚 |
| P4 意图分类/切分 | 强：多意图 A+B 混合分层、SubTask DAG 依赖 | `intent/multi_intent.py` | ✅ 基本达标 |
| P5 规则校验 | 分散：`rulesmatcher`/`tools`(doc-gating)/`safety`(风险分级) | 三个文件各管一段 | ⚠️ 缺集中式四层校验（必填槽→格式→业务→权限） |
| P6 Plan-and-Execute | 有：RoleOrchestrator(4角色SOP) + Orchestrator(DAG) | `roles/`、`core/orchestrator.py` | ⚠️ 缺显式 ReAct 循环（简单任务无轻量路径） |
| P7 SIR 回写/存储判定 | 有回写但**无存储门控**，闲聊也可能进向量库 | `chroma.py:save_memory` | ⚠️ 缺存储漏斗（规则→轻量LLM→丢弃） |
| P8 Output Guard/脱敏 | **完全缺失**（run_qc 是质量打分，不是安全护栏） | 无 | ❌ 需新建 |
| P9 会话归档 | 部分：Trace 表、observation.jsonl、Redis 埋点 | `tracing.py`、`observation.py` | ⚠️ 缺归档/资源清理/熔断检查 |

### 1.2 结构性问题（比缺功能更要命）

1. **两个上帝文件**：`proxy.py`(1791行，SSE+落库+对账+暂停恢复) 和 `core/queue.py`(1588行，意图+分流+clarify/confirm/block/split/pause 全分支)。所有阶段的控制流搅在 queue.py 的 worker_loop 里，改任何一个阶段都可能碰坏别的。
2. **cascade.py 是耦合热区**：import 了 intent/ 下几乎所有子模块，"理解"和"路由决策"混在一起。
3. **阶段无显式边界**：无法回答"这个请求在哪个阶段挂了"——审计日志没有按阶段切分，与总图"出问题直接定位到具体 PHASE"的目标相悖。
4. **无统一上下文对象**：payload dict 一路透传，每个函数按需取 key，字段来源无法追溯。

---

## 二、目标架构

### 2.1 核心设计：Pipeline + Stage + TurnContext

把 worker_loop 的"面条控制流"重构为**显式流水线**：

```
proxy.py (瘦身: 只做 SSE 传输层)
   ↓ enqueue
pipeline.py (总调度: 按序执行 Stage, 每阶段留审计点)
   ├─ S0 gateway/       输入护栏 (injection检测 + 清洗 + 限流)     [纯规则]
   ├─ S1 memory_gate/   召回判定 + 向量召回 + SIR 预填            [规则+轻量LLM]
   ├─ S2 understanding/ 意图理解 → SIR_delta                      [LLM, 复用cascade]
   ├─ S3 dst/           delta 合并 + 快照 + 回滚                  [纯函数]
   ├─ S4 splitter/      单/多意图判定 + TaskList(DAG)             [规则+LLM, 复用multi_intent]
   ├─ S5 validator/     四层校验: 必填槽→格式→业务→权限/风险       [纯规则]
   ├─ S6 executor/      ReAct(简单) / Plan-and-Execute(复杂)       [复用roles+orchestrator]
   ├─ S7 memory_store/  SIR 回写 + 存储漏斗判定                   [规则+轻量LLM]
   ├─ S8 output_guard/  格式校验 + 脱敏 + 一致性校验               [纯规则]
   └─ S9 archive/       审计落盘 + Token统计 + 资源释放 + 熔断      [纯代码]
```

**三条铁律落地为代码约定：**
- 每个 Stage 是独立包，对外只暴露 `async def run(ctx: TurnContext) -> TurnContext`；
- 纯规则阶段 (S0/S3/S5/S8/S9) **禁止 import LLM 客户端**（ruff 自定义规则/import-linter 强制）；
- 每个 Stage 入口/出口自动打点：`ai:stage:{name}:{count|ms|fail}`（复用 analytics.py，遵守 `ai:` 前缀铁律）。

### 2.2 TurnContext：全链路唯一状态容器

```python
@dataclass
class TurnContext:
    # 身份与会话 (S0 填充)
    user: UserIdentity          # user_id/role/vip, 服务端鉴权结果
    session: SessionInfo        # conversation_id/project_id/turn_id
    raw_message: str            # 原始输入
    clean_message: str          # 清洗后输入
    trust: TrustFlags           # untrusted/truncated 标记
    # 记忆 (S1 填充)
    recall: RecallResult        # 召回的画像/知识/episodic + 判定原因
    # 状态 (S2/S3 填充)
    sir_old: SIR | None         # 本轮前快照 (回滚锚点)
    sir_delta: SIRDelta | None
    sir: SIR                    # DST 合并后的真理之源
    # 任务 (S4/S5 填充)
    tasks: list[SubTask]        # DAG TaskList
    validation: ValidationResult
    # 执行 (S6 填充)
    execution: ExecutionResult  # 各步骤结果 + 状态
    # 输出 (S8 填充)
    reply_draft: str
    reply_final: str
    # 审计 (全程追加)
    audit: TurnAudit            # 每阶段 {status, ms, tokens, detail}
```

审计日志按总图 PHASE 9 的 JSON 格式落盘 `logs/session_{id}/turn_{n}.json`，一眼定位哪个阶段慢/挂/贵。

### 2.3 目标目录结构

```
backend/app/
├── main.py                    # 装配 (不变)
├── transport/
│   ├── sse.py                 # 原 proxy.py 的 SSE 传输部分 (~400行)
│   └── session_api.py         # 续传/对账/暂停恢复 (从 proxy.py 拆出)
├── pipeline/
│   ├── pipeline.py            # Stage 编排器 + 短路/降级策略
│   ├── context.py             # TurnContext + SIR dataclass
│   └── stages/
│       ├── s0_gateway.py      # 【新】injection 检测 + 复用 _sanitize_input
│       ├── s1_memory_gate.py  # 【新】召回门控; 调 knowledge/chroma
│       ├── s2_understand.py   # 【改】cascade 瘦身后的适配层
│       ├── s3_dst.py          # 【改】dst.py + 快照/回滚
│       ├── s4_splitter.py     # 【改】multi_intent 适配层
│       ├── s5_validator.py    # 【新】四层校验; 吸收 rulesmatcher/safety/tools 的校验职责
│       ├── s6_executor.py     # 【改】ReAct + Plan 双模式入口
│       ├── s7_memory_store.py # 【新】存储漏斗; 调 knowledge/chroma
│       ├── s8_output_guard.py # 【新】脱敏 + 一致性 + 格式护栏
│       └── s9_archive.py      # 【新】审计落盘 + 熔断
├── agent/
│   ├── intent/                # cascade/dst/multi_intent 保留但瘦身
│   ├── knowledge/             # chroma (不变)
│   ├── roles/                 # 4 RoleAgent (不变)
│   ├── skills/                # 8 技能 (不变)
│   └── schemas/               # 【新】intent_schema 表: 每个意图的必填槽/共享槽/风险级
└── (analytics/metrics/auth/db/repos 不变)
```

**留用不动**：roles/ 角色编排、skills/ 技能、chroma 九集合、analytics 埋点体系、DB/repos 层、前端全部。
**拆掉重组**：proxy.py、queue.py、cascade.py 的路由决策部分。
**净新增**：S0 injection、S1/S7 Memory Gate、S5 集中校验、S8 Output Guard、S9 归档、intent_schema 表、TurnContext。

---

## 三、关键设计决策（逐个说清楚为什么）

### D1. Pipeline 不用 LangGraph，自研 ~200 行编排器
现有代码已是 asyncio 生态，10 个阶段是**线性为主 + 少量短路**（校验失败→直接生成追问跳到 S8），不需要图引擎。自研编排器保持零新依赖、完全可控，且与 SSE 事件流天然兼容（每阶段完成 publish 一个 `stage` 事件，前端可做进度可视化）。

### D2. cascade.py 只留"理解"，路由决策上移
现在 cascade 既做分类又做 route/clarify/confirm/block 决策。重构后：
- cascade → 纯粹的 `classify(ctx) -> SIRDelta`（S2 的内核）；
- clarify/confirm/block/split 的**决策**统一由 S5 validator 产出 `ValidationResult(action=...)`，pipeline 据此短路；
- queue.py 里散落的分支代码全部删除。
建站启发式、PM 粘性、SIR 最小化存储（方案A）等已验证逻辑**原样迁移**，用现有回归脚本守护。

### D3. DST 快照：Redis 环形缓冲，最多 10 轮
`sir:snap:{cid}` = Redis LIST，每次 `apply_delta` 前 LPUSH 旧 SIR，LTRIM 10。支持"还是按刚才的"回滚和审计 diff。纯函数性质不变，新增 `rollback(cid, steps=1)`。

### D4. S5 四层校验以 intent_schema 表为单一真相源
新建 `agent/schemas/intent_schema.py`：每个意图声明 `required_slots / slot_formats / business_rules / risk_level / shared_slots`。
- Level1 必填槽：缺失→生成精准追问（短路到 S8，**不进执行，省 LLM 成本**）；
- Level2 格式：正则/类型校验；
- Level3 业务：日期不早于今天等领域规则；
- Level4 权限：吸收现有 safety.py 的 RISK_HIGH/MEDIUM 分级 + 二次确认。
现有 rulesmatcher/tools/safety 中属于"校验"的逻辑迁入，属于"路由"的逻辑留在 S2/S4。

### D5. S6 双模式执行：ReAct 补位，Plan 保留
- TaskList 仅 1 个 Task 且技能标记 `simple=True`（如问答/查询类）→ **新增 ReAct 轻量循环**（max_steps=5、重复 Action 检测、30s 超时），省掉角色编排的多次 LLM 调用；
- 多 Task 或建站类复杂任务 → 走现有 RoleOrchestrator / DAG Orchestrator，不动。
预期收益：闲聊/简单查询类请求延迟和 token 显著下降（现在部分简单请求也可能过重编排）。

### D6. Memory Gate 双门（S1 召回门 + S7 存储门）
按总图漏斗模型：
- **召回门**：5 条硬规则（新会话必召回/指代词必召回/闲聊不召回…）覆盖 ~60%，模糊区间调轻量模型（qwen 最小档）输出 `no_recall|recall_user|recall_knowledge|recall_episodic`；
- **存储门**：偏好/显式"记住"/任务成败经验→存；闲聊/临时澄清→不存；模糊→轻量LLM 判 `store|weak_store|no_store`，weak_store 带 TTL 30 天 metadata。
这直接解决当前"闲聊污染向量库、召回噪声"的隐患。埋点 `ai:mgate:recall:*` / `ai:mgate:store:*`。

### D7. S8 Output Guard 从零建，纯规则实现
- 格式：拦 JSON/SIR/Thought 泄漏（正则）；
- 脱敏：手机号/邮箱/身份证/订单号分级打码（可配置开关，内部测试期默认只做格式+一致性）；
- 一致性：回复中的订单号/价格/URL 必须能在 ctx.execution 中找到出处，找不到→以工具结果为准修正或删除。
不调 LLM，失败兜底走模板回复。

### D8. proxy.py 三拆
1791 行拆为：`transport/sse.py`（SSE 收发+断连取消 C1 逻辑）、`transport/session_api.py`（续传/对账/暂停）、鉴权配额部分并入 pipeline S0。SSE 断连自动取消(C1)、`clients:<tid>` 计数等已验证机制**原样保留**。

---

## 四、实施计划（6 个里程碑，每个独立可 commit、可回归）

> 原则：**绞杀者模式**——新 pipeline 与旧 worker_loop 并存，用 `PIPELINE_V3_ENABLED` 环境变量切换（默认关），每个里程碑跑回归后再推进；全部完成后删除旧链路。

### M1：地基——TurnContext + Pipeline 骨架 + S9 审计
- 新建 pipeline/ 包、TurnContext、Stage 协议、编排器；
- 先把 S9 审计做出来（每阶段自动打点 + turn JSON 落盘），后续每个里程碑天然可观测；
- 旧链路整体包一层"单一巨型 Stage"跑在新编排器里 → 行为零变化，验证骨架。
- 验收：`scripts/run_tests.py --quick` 全绿；`logs/session_*/turn_*.json` 生成。

### M2：入口——S0 gateway + proxy 三拆
- injection/越狱检测规则集（关键词+模式，标记 untrusted 降级而非硬拒）；
- proxy.py → transport/ 三拆，SSE/C1 逻辑回归验证。
- 验收：`_verify_sir_http.py` 通过；SSE 断连取消行为不变；注入样例集(新增 ~30 条)全部标记。

### M3：状态——S2/S3/S4 迁移 + DST 快照
- cascade 瘦身为纯理解内核，路由决策剥离；multi_intent 接入 S4；
- DST 快照/回滚上线；
- 这是风险最高的一步，靠三套回归守护：`multi_intent_regression.py`(24断言)、`_diag_template_route.py`、`_verify_sir_http.py`，另补 DST 快照单测。
- 验收：三套回归全绿 + 意图分布对比（重构前后各跑 200 条 `run_tests.py`，`ai:` 埋点意图分布偏差 < 2%）。

### M4：门禁——S5 validator + intent_schema 表
- 建 intent_schema（先覆盖 8 个现有技能对应意图）；
- 四层校验落地，clarify/confirm/block 决策统一出口；
- queue.py 相应分支删除。
- 验收：缺槽追问用例集（新增 ~20 条）；风险确认流程手测；旧分支代码删除后回归全绿。

### M5：记忆与执行——S1/S7 Memory Gate + S6 ReAct
- 双门上线（先规则层，轻量 LLM 兜底二期再开）；
- ReAct 轻量循环补位简单任务；
- 验收：闲聊类消息 0 入库（查 Chroma 写入日志）；简单查询类请求 LLM 调用次数下降（`ai:stage:s6:*` 对比）。

### M6：出口与收尾——S8 Output Guard + 删旧链路 + 文档
- Output Guard 上线（格式+一致性必开，脱敏可配置）；
- `PIPELINE_V3_ENABLED` 默认开 → 稳定后删除旧 worker_loop 全部分支代码；
- 跑 `_e2e_20_abcde.py` 全量 E2E（固定账号 e2e20_seedai_test/testpass123），出测试报告（含测试账号段）；
- 同步 OPTIMIZE_PLAN.md 版本记录。
- 验收：E2E 20 场景通过；ruff+mypy 门禁全绿；上帝文件消灭（单文件 < 800 行）。

### 里程碑依赖与回滚

```
M1 → M2 → M3 → M4 → M5 → M6
每个 M 完成即本地 commit；M2~M5 期间任何异常 → PIPELINE_V3_ENABLED=0 秒级回退旧链路。
```

---

## 五、风险清单

| 风险 | 等级 | 对策 |
|---|---|---|
| M3 迁移 cascade 时丢失已验证的启发式（建站共现/简历口吻/SIR方案A） | 高 | 逻辑原样搬运不重写；三套现有回归脚本 + 新增快照单测全程守护 |
| 新旧链路并存期间行为不一致难排查 | 中 | S9 审计先行(M1)，新链路每阶段有 JSON 留痕；旧链路仅作回退不再改动 |
| Memory Gate 误判导致该召回没召回 | 中 | 规则层先保守（宁多召回勿漏），轻量 LLM 兜底放二期；`ai:mgate:*` 埋点观察一周再收紧 |
| ReAct 路径与技能体系的接口不匹配 | 中 | 仅对标记 simple 的技能启用，逐技能白名单放开 |
| 重构周期内新需求插入造成两头改 | 中 | 并存期新功能一律只在新 pipeline 上实现；旧链路冻结 |
| Redis 键膨胀（快照/新埋点） | 低 | 快照 LTRIM 10 + TTL；埋点沿用 analytics.py 既有聚合方式 |

---

## 六、验收总标准

1. **功能不回退**：`run_tests.py` 200 条、`multi_intent_regression.py` 24 断言、`_e2e_20_abcde.py` 20 场景全部通过；
2. **结构达标**：无 800 行以上业务文件；纯规则阶段零 LLM import（import-linter 校验）；
3. **可审计**：任意一次请求可在 `logs/session_*/turn_*.json` 看到十阶段耗时/状态/token；
4. **新能力生效**：injection 样例被标记、缺槽先追问不进执行、闲聊不入向量库、回复无内部状态泄漏、SIR 可回滚；
5. **成本可见**：`ai:stage:*` 埋点能回答"哪个阶段最贵/最慢"。

---

## 七、待你拍板的三个问题

1. **脱敏默认开还是关？** 建议内部测试期默认关（只开格式+一致性校验），上生产前再开。
2. **轻量 LLM 兜底用哪个模型档位？** Memory Gate 模糊判定建议用 qwen 最小档（如 qwen-turbo），单次 < 100 token。
3. **M3 前是否先补一轮基线数据？** 建议重构开工前先跑一次 200 条回归 + 保存 `ai:` 埋点快照，作为 M3/M6 对比基线。

---

> 文档版本 v1.0 · 2026-07-30
> 蓝本：《Agent 全链路执行总图·完整详版》v2.0
> 摸底依据：backend/app 全量代码探索（proxy.py/queue.py/cascade.py/dst.py/multi_intent.py/roles/ 等）
