# SeedAI 角色重构：方案 A vs 方案 B 对比（OPTIMIZE_PLAN §4）

> 作者：Senior Developer｜日期：2026-07-27
> 背景：用户在第 4 点（角色重构）选了 **方案 B**，但忘记了 **方案 A** 的具体内容，要求补做对比。
> 重要**：本文件中的「方案 A」为从设计讨论上下文_重建_的，OPTIMIZE_PLAN.md §4 当时只保留了选定的 B-轻 全文。如与你的记忆不符，请直接纠正（见文末 §4）。

---

## 1. 两个方案的真实形态

### 方案 A（重建 · 最可能为「维持扁平 / 原地优化」）
- **不正式拆出** ProductAgent / DesignAgent / DevAgent / QAAgent 四个角色实体。
- 保留现有 8 个 skill（`chat / search / design / doc / build / requirement / review / generate_site`）的**扁平结构**，角色边界只靠 system prompt 里的「你现在是产品/设计角色」约定，无独立上下文。
- 只做**增量优化**：意图路由增强、编排串联（planner→merge）、必要时在现有 skill 间加一层薄 orchestrator 函数。
- 聊天历史对执行体**完全可见**，没有「只注入上游交接物」的限制。
- 交接物 = 自然语言 / 局部 JSON，**无强 Schema 约束**（如 PRD/DesignSpec 是 prompt 里写、代码里不建模）。
- 改动量：小，主要在 `router.py / orchestrator` 一层；不动 skill 内部。

### 方案 B-轻（已选定 · 文档化于 OPTIMIZE_PLAN §4）
- **正式拆 4 个 RoleAgent**（Product/Design/Dev/QA），每个是单进程内的逻辑角色，`system prompt` 锁定职责边界、禁止越权。
- **上下文隔离**：每个 RoleAgent 仅注入 ① 自身 prompt ② 上游交接物（而非整段聊天）③ 最小项目上下文。Dev 不被闲聊污染，QA 严格按 PRD 验收。
- **强 Schema 交接物协议**：`PRD / DesignSpec / CodeArtifact / ReviewReport` 用 TypedDict/Pydantic 建模，落 `app/agent/roles/handoff.py`。
- **执行层复用**现有 8 skill（RoleAgent 包装 handler，不重写逻辑），仅**重写编排层**（Orchestrator + handoff）。
- **守单进程铁律**（v2.0.0 起）：不退回双进程/多服务 IPC。

> **备选解读（若你的 A 是另一种）**：A 也可能是「**重拆 4 独立进程/服务**（B-重的正式命名）」——每个角色真独立部署、靠消息队列/RPC 通信、可独立扩容/计费/告警。该形态在 B-轻 §4.6 里作为「未来升级路径」被提及并否决（会推翻单进程铁律）。若你记忆中的 A 是这种，请见 §4，B-轻 已预留升级到它的接口（transport 从 in-process 换 queue/RPC，交接物协议不变）。

---

## 2. 维度对比表

| 维度 | 方案 A（维持扁平/原地优化） | 方案 B-轻（已选定） |
|---|---|---|
| 角色边界 | 靠 prompt 约定，软隔离 | 4 个 RoleAgent + 职责锁定，硬边界 |
| 上下文隔离 | 无；整段聊天历史对 skill 全可见 | 仅注入上游交接物 + 最小上下文 |
| 交接物 | 自然语言 / 局部 JSON，无 Schema | 强 Schema（PRD/DesignSpec/CodeArtifact/ReviewReport） |
| 对现有 8 skill | 直接用，不改内部 | 包装为 RoleAgent 执行体，不重写逻辑 |
| 单进程铁律 | ✅ 天然守（什么都不拆） | ✅ 守（同进程逻辑隔离） |
| 改动范围 | 小（仅路由/编排层增量） | 中（新增 roles/ + handoff.py + orchestrator 重写） |
| 实现风险 | 低 | 中（交接物建模 + 上下文裁剪需调参） |
| 可独立评测 | ❌ 角色耦合，难单测 | ✅ 四角色可单独评测/单独换模型 |
| 代码质量闭环 | 弱（验收靠人看） | 强（QA 按 PRD 结构化验收） |
| 技能复用/迭代 | 一般 | 高（执行层与编排层解耦） |
| 回归成本 | 低 | 中（需补 RoleAgent 单测 + 闭环回归） |
| **核心价值** | 快、稳，但不解决「角色串味/验收无据」 | 专业流程 SOP，质量可度量、可演进 |

---

## 3. 为什么选 B-轻（结合本轮诊断）

本轮 20 条全链路测试暴露了「质量短板」：多意图召回不稳、文档类未独立路由、验收无结构化依据。这些问题**方案 A 解决不了**——它只是把现有扁平结构调顺，角色边界仍然模糊（Dev 会被闲聊污染、QA 没有 PRD 可对照验收）。

方案 B-轻 用「上下文隔离 + 强 Schema 交接物」直接补上这三块短板，且**不破坏单进程架构**（这是你 v2.0.0 定的铁律，A-重 / B-重会推翻它）。

结论：**B-轻 是"体验目标（拆角色）+ 架构纪律（单进程）"的唯一交集解**，A（无论哪种解读）要么不改流程、要么推翻铁律，均不如 B-轻。

---

## 4. 请你确认（重建校验）

1. 你记忆中的 **方案 A** 是哪一种？
   - **(a)** 维持扁平、原地优化（不拆角色）← 我按此重建为主；
   - **(b)** 重拆 4 独立进程/服务（B-重）← 见 §1 备选解读。
2. 若 A=(a)：当前 B-轻 已是最优，无需改动，§4 角色重构按已定计划单独排期实现即可。
3. 若 A=(b)：B-轻 §4.6 已预留升级路径（交接物协议不变，仅 transport 换 queue/RPC），等你真要"每角色独立部署/独立计费"时再升级，不阻塞当前工作。

> 无论 A 是哪版，本轮都不实现角色重构（你已定：第 4 点单独排期），本文件仅为决策回顾。
