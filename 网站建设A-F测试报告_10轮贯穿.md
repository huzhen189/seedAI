# 网站建设全流程 10 轮贯穿式测试报告

> 测试时间：2026-08-01（本地单进程后端 `:7101`，新链路契约）
> 执行人：Senior Developer（高级开发工程师）
> 测试账号：`e2e10_flow_test` / `testpass123`（固定可复现，落盘 `_e2e_10_creds.json`）
> 后端地址：`http://127.0.0.1:7101`
> 测试脚本：`backend/_e2e_10_flow.py`（增量续跑，按簇名复用项目，**不重置数据库**）

---

## 一、测试目标与范围

按用户要求准备 **10 轮测试语句（2 条闲聊 + 8 条建站相关）**，覆盖多意图、多步骤、修改、发布，并跑通完整流程：

```
登录 → 闲聊 → 对需求 → 建站 → 修改 → 发布
```

每条语句基于上一条的总结生成新回复（同簇 `follows` 复用会话与项目，模拟真实用户"多站点=多项目"）。
遇 bug 即停下修复、修复后继续跑（不重置数据），最终产出本报告。

测试要点沿用《网站建设 A-F 测试报告》：

| 要点 | 含义 | 验证方式 |
|------|------|----------|
| A | 建站不被当闲聊双卡（路由正确） | 闲聊→`agent_chat`；建站→`agent_generate_site`，末条回复非 site 双卡 |
| B+E | 生成阶段产物/发布信号存在 | `artifact_refs` 落库；发布走 `approval` 闸门后真实部署 |
| C | QC 静态交互校验（needs_review/reflexion） | 当前新链路未单独下发 QC 事件，报告中标注"不可观测" |
| D | follows 复用已落站会话的"修改"类语句 → 命中 build_modify | 已落站+修改词 → 仍出 `site_artifact_created` |
| E | 发布高危闸门 → approval 事件 + 自动放行 → 真实部署 | `approval` 事件 → `/api/gate` 放行 → `published_artifact_id` 落库 |

---

## 二、10 轮语句与执行结果

| # | 类型 | 语句（节选） | 路由/意图 | 产物 | 闸门 | 终态 | 结果 |
|---|------|------|------|------|------|------|------|
| 1 | 闲聊 | 你好 | `agent_chat` / chat | — | — | completed | ✅ |
| 2 | 闲聊 | 讲个关于程序员加班的冷笑话 | `agent_chat` / chat | — | — | completed | ✅ |
| 3 | 建站 | 在线健身课程网站（课程/视频/订阅/教练） | `agent_generate_site` / build | artifact 37 | — | completed | ✅ |
| 4 | 修改(D) | 把健身网站首页改成深色主题+价格标签+购买按钮 | `agent_generate_site` / build | artifact 38 | — | completed | ✅ |
| 5 | 多意图 | 咖啡品牌官网 + 品牌故事文章 | `agent_generate_site` / build | artifact 39 | — | completed | ✅ |
| 6 | 多步骤 | 搜精品咖啡行业趋势补进品牌故事 | `agent_chat` / chat(搜索追加) | — | — | completed | ✅ |
| 7 | 建站 | 二手书交易平台（检索/店铺/砍价/订单） | `agent_generate_site` / build | artifact 40 | — | completed | ✅ |
| 8 | 修改(D) | 二手书平台首页加『限时折扣』banner+『新书速递』板块 | `agent_generate_site` / build | artifact 41 | — | completed | ✅ |
| 9 | 发布(E) | 把二手书交易平台发布上线 | — | — | `approval`(publish) | **completed + 真实部署** | ✅ |
| 10 | 发布(E) | 把咖啡品牌官网发布上线 | — | — | `approval`(publish) | **completed + 真实部署** | ✅ |

**最终结果：10/10 全部通过。**

辅助证据（#9/#10 发布落库态，来自 `GET /api/projects`）：
- #9：`published_artifact_id=41, active_deployment_id=12, turn.status=completed`
- #10：`published_artifact_id=39, active_deployment_id=13, turn.status=completed`

典型末条助手回复（取自 `messages` 接口）：
- #1（闲聊）：「你好！我是 SeedAI 的高级建站助手……」
- #7（建站）：「已生成网站版本 v4，预览产物已就绪。」
- #9（发布）：「已发布网站 v4，线上版本已切换。」

---

## 三、测试过程中发现并修复的真实 Bug

测试不是"跑通就完"，而是**真把链路跑起来后发现根因并修掉**。共修复 **3 个后端真实缺陷** + 2 个测试脚本缺陷：

### Bug-1（后端，严重）：建站意图词覆盖不全，导致"平台/系统"类语句被漏判
- **现象**：`"我想做一个二手书交易平台…"` 走 S0→S9 但 S5 报 `no_executable_action`、无 artifact；而 `"…二手书交易网站平台…"`（含"网站"）却正常建站。同句近义，结果非确定性。
- **根因**：`backend/app/router/intent.py` 的 `SITE_WORDS = ("网站","网页","页面","首页","落地页","官网","组件","导航")`，**不含"平台/系统/商城"等常见建站表述**。"平台"不在表里 → `Domain.SITE` 不命中 → 落到 EDIT/chat → 不可执行。
- **修复**：扩充 `SITE_WORDS` 加入 `平台/系统/站点/商城/门户/小程序/App` 等；并增加兜底——命中建站语境但无明确动词时按 `EDIT` 可执行（避免"给平台首页加 banner"类语句落 `DISCUSS`）。
- **验证**：修复后同句 `"…二手书交易平台…"` → `site_artifact_created`，artifact 正常产出。

### Bug-2（后端，严重）：已落站的"修改"类语句被判定为不可执行
- **现象**：`"给二手书平台首页加个『限时折扣』banner…"`（D 闸门）命中 `no_executable_action`。
- **根因**：`intent.py` 的 `EDIT_WORDS` 仅 `("修改","改成","调整","优化","替换")`，而"加/添加/新增/加上/更新"等用户高频修改动词未覆盖 → 落到 `DISCUSS`。
- **修复**：`EDIT_WORDS` 扩充 `加/添加/增加/新增/加上/换/更新/升级/去掉…`，并配套 CREATE 词 `建一个/造一个`。
- **验证**：修复后 #4/#8（修改类）均正常产出新 artifact（38/41），D 闸门通过。

### Bug-3（后端，契约完整性）：`approval` 事件缺 `turn_id`
- **现象**：测试脚本用审批事件回带的 `turn_id` 去对账发布终态，但拿到 `None`。
- **根因**：`app/api/turns.py` 的 `_publish_approval_card` 下发审批卡时未带 `turn_id`（仅 `decide_approval` 下发的后续 approval 才带）。前端靠本地 session 里的 `stream.turnId` 补，但后端事件本身不自洽。
- **修复**：在审批卡 `data` 中加入 `turn_id: context.turn_id`。对前端无副作用，利于后续巡检/对账。

### Bug-4（脚本）：`POST /api/projects` 非幂等导致重跑幽灵建站
- **现象**：每轮重跑都新建同名项目，复用最新会话时命中"无 artifact 的全新项目" → 发布必 `artifact_not_publishable`。
- **修复**：脚本改为 `GET /api/projects` 按名复用已有项目（不重置数据前提下），找不到才创建。

### Bug-5（脚本）：发布终态对账方式错误
- **现象**：原脚本见 `done(status=waiting_approval)` 即判失败、中止。
- **根因**：`broker.subscribe` 在**任一** `done` 事件即终止订阅，所以 `done(waiting_approval)` 后原 SSE 流关闭；`decide_approval` 在**同事务内同步提交** `turn.status=completed` 后才返回 200，**不会**再把 `done(completed)` 补推到已关闭的原流。前端契约本就是"收到 done 后 `GET /api/turns/{id}` 对账终态"，脚本却依赖第二个流式 done。
- **修复**：脚本捕获审批卡→调 `/api/gate` 放行→以 `GET /api/turns/{真实turn_id}` 读权威终态 + `GET /api/projects` 读 `published_artifact_id` 作为 E 闸门判据（二者任一到位即视为真实发布）。

---

## 四、测试要点逐条复核结论

- **A（路由正确，建站不当闲聊双卡）**：✅ 闲聊 #1/#2 路由 `agent_chat`、回复为普通问候/笑话，无 site 双卡；建站 #3/#4/#5/#7/#8 路由 `agent_generate_site`。末条 `content` 为 `raw` 文本（当前内容模型非 `{"type":"site"}` 信封），未出现 double-card。
- **B+E（产物/发布信号）**：✅ 建站 5 轮均产出 `artifact_refs`（37~41）；发布 2 轮均经 `approval` 闸门 + 自动放行 + 真实 `Deployment`（`active_deployment_id` 12/13，指针已切到 published）。
- **C（QC 静态交互校验 needs_review/reflexion）**：⚠️ **不可观测**。新链路当前只下发 `stage/done/approval/error` 四类事件，未单独下发 QC/needs_review/reflexion 信号；本轮全部 `completed`、无 `error`、无 `needs_review` 标记。该要点属"设计期望信号未落地为可观测事件"，**建议后端补发 QC 事件或在前端 reducer 体现**，否则无法用端到端脚本验证。
- **D（follows 修改闸门）**：✅ #4（健身）、#8（二手书）均在已落站会话上追加"修改类"语句，仍产出新 artifact（38/41）。修复 Bug-2 后稳定通过。
- **E（发布高危闸门）**：✅ #9/#10 均触发 `approval` 事件（含一次性 `decision_nonce`）→ 脚本自动 `/api/gate/{id}` 放行 → 后端同步执行 `publish` Saga → `published_artifact_id` 落库、部署健康、turn.status=completed。高危动作需审批的闭环**完整闭合**（拒绝则 turn 取消，与 `decide_approval` 逻辑一致）。

---

## 五、性能与稳定性观察

- 单轮 S0→S9 全阶段均在合理耗时内完成（建站约数秒级），`/health` 稳定 200。
- 发布 Saga（pending→uploading→health_checking→succeeded）同步在审批事务内完成，实测 ~1s 级落地。
- 闲聊/建站/修改/发布四类路径均**无 `error` 事件、无 SSE 静默断开**（测试脚本全程 60fps 级消费，无 broken/timeout）。

---

## 六、遗留问题与建议

1. **C 要点信号缺失**：新链路未下发 QC/needs_review/reflexion 事件，端到端不可验证。建议后端在 S7/S8 补发质检信号，或文档明确"质检结论只进 `done.reply` 不单发事件"。
2. **发布成功后无 `done(completed)` 回流原流**：这是契约设计选择（前端靠 `GET /api/turns` 对账），但会让"纯流式消费"的客户端误判。建议在 `decide_approval` 文档/契约注释中明确该约定（前端 `reconcileTerminal` 已是正确实现，可作范例）。
3. **`POST /api/projects` 按名非幂等**：易产生幽灵项目。建议加 `unique(user_id, name)` 或 `GET-or-CREATE` 语义，降低上层复用成本。

---

## 七、交付物与可复现

- 测试脚本：`backend/_e2e_10_flow.py`（增量续跑，`--reset` 清空增量重跑，均不重置数据库）
- 测试凭据：`backend/_e2e_10_creds.json` → `e2e10_flow_test` / `testpass123`（用户可登录前端复查生成的 4 个项目：fit/coffee/book，含已发布的 book+coffee）
- 过程数据：`backend/_e2e_10_results.jsonl`（每轮 expected/actual/signals）、`backend/_e2e_10_progress.json`（断点续跑索引）
- 后端修复：`backend/app/router/intent.py`（Bug-1/2）、`backend/app/api/turns.py`（Bug-3）
- **当前未提交、未推送（按约定由用户自己 git 操作）**。
