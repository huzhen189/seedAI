# 网站建设流程改进方案（方案先行 · 确认后进开发）

> 文档状态：待评审（先对齐方案，再进入开发）
> 适用模块：`backend/app/agent/skills/agent_generate_site.py`、`agent_requirement.py`、`user_state.py`、前端 ChatView 方案确认 UI
> 关联问题：用户发送"我想做一个电商网站，要有商品列表页和购物车功能"后——没出文档、流程卡住、日志报错

---

## 一、当前问题定位（根因，来自真实日志）

### 1.1 需求源被"过时摘要"劫持（核心 bug）
- `agent_generate_site.py:456 _select_requirement` 的优先级是：
  1. 结构化需求文档 → 2. **`conversation_summary`** → 3. 最近含需求语义的用户消息 → 4. 兜底最后消息
- 本次请求（trace `t19fa3ab403a940c9c269ddbf`，21:02–21:03）中，`conversation_summary` 是**过时的**：内容为"用户请求翻译『Hello World』至中文，因服务重启中断"。
- 于是 Planner 基于错误摘要，生成了完全跑题的方案：**「小胡AI对话」——一个模拟 AI 助手对话、展示"服务中断"状态的静态界面**（连 think 里都写了"因平台限制无法接入真实后端，采用纯前端状态机模拟"）。
- 日志实锤：`[gen] 需求来源=conversation_summary 长度=66`，Planner `title=小胡AI对话`，`goal=构建模拟用户与AI助手小胡对话的静态界面…`。

### 1.2 流程卡在 await_confirm，且方案错误 → 体感"卡死"
- 生成流在 `paused(stage=await_confirm)` 暂停，等用户确认方案（21:03:02）。这是**设计内的闸门**，但：
  - 方案是错的（见 1.1），用户自然不愿确认；
  - 前端若未对 `paused` 弹出明确"确认/修改"卡片，就会表现为"流程一直卡住"。
- 关键：确认前**根本没进到 Coder 出 HTML 文档**那一步，所以"没有看到文档"。

### 1.3 日志报错：user_state 重置失败
- `backend/logs/app.log` 20:41–20:56 反复 `WARNING [app.user_state] reset MySQL 失败 uid=1: (pymysql.err.IntegrityError) (1048, "Column 'progress_pct' cannot be null")`。
- 根因：`user_state.py:156 _RESET_FIELDS` 含 `progress_pct`，`reset_user_state`(:198-200) 对它 `setattr(row, f, None)`；而 DB 列 `progress_pct` 为 **NOT NULL** → 每次 `reset_all` 清库都炸，user_state 重置静默失败（孤儿状态可能残留）。

### 1.4 摘要本身健壮性差（次级隐患）
- 21:02:19 `摘要过期回退重压 conv=2 len=66` → 重压结果只保留了"翻译 Hello World"语义，**丢掉了最新的电商需求消息**。
- 21:03:05 `对话摘要已更新 conv=2 len=0` → 最终摘要竟更新成**空串**。
- 说明 `conversation_summary` 是 LLM 有损压缩，会丢消息、会变空，绝不能作为需求真相源。

---

## 二、目标流程：三阶闸门（方案先行，确认后进开发）

把"网站建设"拆成 3 个清晰闸门，**每一道闸门都必须产出"可被用户看见、可确认、可回退"的产物**，确认后才进下一道。

```
用户指令
   │
   ▼
┌─────────────────────────┐
│ 闸门① 需求 (Requirement) │  拿到"明确、完整、新鲜"的需求
│  优先级: 最新明确指令 >   │  - 最新一条用户指令为最高权威
│  结构化文档 > 摘要(补充)  │  - 指令过短/残缺 → 触发澄清补全
└───────────┬─────────────┘
            │ 需求明确
            ▼
┌─────────────────────────┐
│ 闸门② 方案 (Plan)        │  产出结构化 Plan 卡片
│  title/goal/steps        │  - 前端渲染"确认 / 修改"双按钮
│  + 页面/功能清单          │  - 可编辑、可重规划（回退到①）
└───────────┬─────────────┘
            │ 用户确认
            ▼
┌─────────────────────────┐
│ 闸门③ 开发 (Develop)     │  Coder 产出真实 HTML 文档
│  落库为 artifact         │  - 绝不"模拟/演示"代替
│  + SSE 续接/回放         │  - 沿用已修的断点续接
└─────────────────────────┘
```

### 设计原则
1. **新鲜度优先**：用户"最新一条明确指令"永远是需求最高权威；`conversation_summary` 只作长期上下文补充，**绝不替代**。
2. **不信任压缩**：摘要是有损的（已证实会丢消息、变空），只补充不替代。
3. **闸门可回退**：需求阶段可重新澄清，方案阶段可改可重规划；确认后才不可逆进入开发。
4. **每个终态都落 message**（沿用 #447）：成功/失败/中断/放弃都要有反馈，不悬空假死。

---

## 三、具体改动清单（供后续开发对照）

### A. 【核心】重排 `_select_requirement` 优先级（修 1.1）
`backend/app/agent/skills/agent_generate_site.py:456`

- 把"最新含建站/内容语义的用户消息"提到 `conversation_summary` **之前**；摘要只在"用户消息是空指令"时作补充。
- 候选检测放宽：含 `_BUILD_KW` **或** `_CONTENT_KW` 即视为需求消息（避免漏掉"商品列表页/购物车"这类描述）。

```python
def _select_requirement(requirement_doc, conversation_summary, messages):
    # 1) 结构化需求文档(最权威)
    if isinstance(requirement_doc, dict) and requirement_doc:
        report = requirement_doc.get("report")
        if isinstance(report, str) and report.strip():
            return report.replace("\\n", "\n"), "requirement_doc"
        return _req_doc_to_text(requirement_doc), "requirement_doc"

    # 2) 最新一条"含建站/需求语义"的用户消息 —— 最新明确指令优先于可能过时的摘要
    candidates = []
    for m in messages:
        if m.get("role") != "user":
            continue
        c = m.get("content") or ""
        if not isinstance(c, str):
            continue
        has_build = any(kw in c for kw in _BUILD_KW)
        has_content = any(kw in c for kw in _CONTENT_KW)
        if has_build or has_content:
            score = (2 if has_build else 0) \
                    + sum(1 for kw in _CONTENT_KW if kw in c) \
                    + len(c) // 40
            candidates.append((c, score))
    if candidates:
        candidates.sort(key=lambda x: x[1], reverse=True)
        best = candidates[0][0]
        # 仅当用户指令过短(纯指令, 如"生成网站")才回退摘要补充上下文
        if len(best) < 12 and isinstance(conversation_summary, str) and conversation_summary.strip():
            return conversation_summary.strip(), "conversation_summary"
        return best, "user_message"

    # 3) 无任何需求语义消息 → 回退对话摘要
    if isinstance(conversation_summary, str) and conversation_summary.strip():
        return conversation_summary.strip(), "conversation_summary"
    # 4) 兜底: 最后一条用户消息
    for m in reversed(messages):
        if m.get("role") == "user":
            c = m.get("content") or ""
            if isinstance(c, str) and c.strip():
                return c, "user_message"
            break
    return "", "none"
```

> 效果：本次"电商网站+商品列表+购物车"含 `_BUILD_KW('网站')` + `_CONTENT_KW('商品''列表''购物车''功能')` → 高优先级直接选中，过时摘要被忽略。
> 回归：RC1 场景（"首页天气+附近美食+地图定位" + "按我刚刚的要求生成网站"）仍正确——前者含 `_BUILD_KW('首页')`+`_CONTENT_KW`，优先被选中；纯指令"生成网站"因 `len<12` 回退摘要补充。

### B. 【方案先行关键】build/site 前置"需求闸门"
- 在 `runner.py` 分发 `agent_generate_site` 前，判断"需求是否完整"：
  - 若有结构化 `requirement_doc` → 直接进生成。
  - 若最新用户消息过短/无内容语义（如"生成网站""就按这个做"）且无可结构化文档 → **先调 `agent_requirement` 澄清补全**，产出结构化 `report` 后再进入生成，**绝不基于残缺摘要直接开干**。
- 这样"先整理清楚需求"在系统层被强制保证。

### C. 【方案先行关键】Plan Gate 前端化（修 1.2）
- `await_confirm` 必须携带**结构化 plan**（title/goal/steps + 页面/功能清单），前端统一渲染"确认 / 修改"双按钮卡片。
- 确认超时或用户放弃 → 后端落**明确终态 message**（如"已取消本次方案，可重新描述需求"），不悬空假死。
- 前端对每个 `paused` 状态都必须有可见 UI（当前"卡住"很大程度是前端没弹确认卡片）。

### D. 【修日志报错】`progress_pct` 重置失败
`backend/app/user_state.py`

两选一（推荐 D1，免迁移）：
- **D1（代码层）**：`reset_user_state` 对 `progress_pct` 置 `0` 而非 `None`（"进度归零"语义正确，且 0 满足 NOT NULL）：
  ```python
  if row is not None:
      row.status = "idle"
      for f in _RESET_FIELDS:
          if f == "progress_pct":
              setattr(row, f, 0)          # NOT NULL 列不能置 None
          elif hasattr(row, f):
              setattr(row, f, None)
      await s.commit()
  ```
- **D2（schema 层）**：`ALTER TABLE user_states MODIFY progress_pct INT NULL;`（需迁移脚本，影响面更大）。

### E. 【健壮性】摘要不再当需求真相源
- 已通过 A 缓解（摘要仅补充）。
- 另建议：摘要重压时必须包含"最新一条用户消息"（修 1.4 的 len=66 丢消息）；最终更新不允许写出空串（修 1.4 的 len=0）。

### F. 【可观测】诊断信息外露（联调友好）
- 在 SSE 或 `get_conversation_status` 接口返回"当前需求来源 / 需求文本前 80 字 / 摘要前 80 字"，联调时一眼能看到用的是哪条需求，避免再被过时摘要带偏而无从排查。

---

## 四、验证用例（开发完成后逐条核对）

| 用例 | 操作 | 期望结果 |
|---|---|---|
| V1（修 1.1） | 历史含旧翻译请求 + 最新"电商网站，商品列表页+购物车" | 基于最新消息生成**电商站**，而非"小胡AI对话"demo |
| V2（修 A 回退） | 仅说"生成网站"（空指令）+ 摘要含电商需求 | 方案仍正确（摘要补充）或触发澄清 |
| V3（方案先行） | 进 Plan 阶段 | 弹出可读"确认/修改"卡片；确认后才出 HTML；不确认有明确保态，不空转 |
| V4（修 D） | 跑 `reset_all` | user_state 重置**无** `progress_pct` 报错 |
| V5（续接） | 生成中 F5 / 手动暂停续跑 / 硬杀重启 | 三种状态全链路正确（沿用已落地的 SSE 续接） |

---

## 五、实施建议（节奏）
1. **第一批（止血）**：A + D —— 直接消除"跑题方案"和"日志报错"两个最痛症状，风险低、可独立上线。
2. **第二批（方案先行）**：B + C —— 把"需求闸门 + 方案确认 UI"做实，真正满足你"先整理清楚方案再进开发"的诉求。
3. **第三批（打磨）**：E + F —— 摘要健壮性 + 诊断可观测，降低后续联调成本。

> 以上为方案文档，待你确认后我再进入开发环境逐批落地。
